# 2026-07-25：起点 oracle evidence 因果 screen 结果

## 今日结论

Seed 42、2 epochs 的 observation-bounded oracle evidence screen 在共同评价集合上**方向性通过**：`val_unseen` 的 matched condition 同时显著超过 null、within-scene shuffle 与 global shuffle 的 overall Cell F1 和 Raster IoU，并在 pooled unobserved cells 上保持更高 F1/IoU。这说明模型确实利用了与当前起点匹配的可见语义，而不只是依赖 instruction 或任意 scene-level semantic inventory。

但这还不是最终因果结论，也不足以进入 Try5 navigation：

- 只有一个 training seed；预设结论仍要求至少三个 paired training seeds。
- unobserved 增益由 precision 主导：matched recall 为 0.1336，低于 within-scene 的 0.1690，不能表述为“恢复了更多未知 cell”。
- 当前 scene-cluster bootstrap 只覆盖 episode-macro F1/IoU、类别、方向与 schema validity，不覆盖 pooled observed/unobserved 或 union 指标；后两者目前只有描述性 effect size。
- evaluator 的 `prediction_evidence_union` 是“当前 condition 的 prediction ∪ recipient prompt semantics”，不是此前文档所指的独立 legacy predictor union。Matched 虽超过这个 deterministic post-processing baseline，但严格的 legacy-union contract 尚未完成。
- Remote launcher 只给 within-scene condition 加了 common-population 参数，导致原始 `val_seen` 的 matched/null/global 使用 778 条，within-scene 使用 769 条。本地已对四个 condition 统一重评为 769/1,839；数值影响很小，但原始 seen 表格不能直接作为严格 paired comparison。

因此当前最准确的结论是：**matched 起点语义提高了 unseen layout prediction 的选择性与精度，single-seed causal screen 成功；是否稳定改善 completion recall 和跨 seed 泛化仍待验证。**

## Slurm 与训练审计

| Job | 任务 | 状态 | 耗时 | Batch MaxRSS |
|---|---|---:|---:|---:|
| `1182802` | matched training | COMPLETED `0:0` | 09:09:00 | 57,396,476 K |
| `1182803_0` | matched cache/eval | COMPLETED `0:0` | 19:55:58 | 6,522,616 K |
| `1182803_1` | null cache/eval | COMPLETED `0:0` | 01:28:56 | 7,002,312 K |
| `1182803_2` | 原 within-scene | FAILED `1:0` | 00:01:30 | 3,530,332 K |
| `1182803_3` | global cache/eval | COMPLETED `0:0` | 21:40:52 | 7,466,984 K |
| `1184467_2` | replacement within-scene | COMPLETED `0:0` | 21:00:11 | 6,144,496 K |

- Array-level `1182803` 显示 COMPLETED 不能覆盖 task 2 的旧 failure；真正可用的是 replacement `1184467_2`。相关任务已经全部离开 `squeue`。
- 四个成功日志没有 traceback、OOM 或 fatal error。严格 parser/write failures 来自 invalid JSON、malformed/out-of-bounds cells 或 prediction keys 不匹配；这些 raw outputs 没有从 denominator 删除。
- 所有 condition 的 evaluator `missing_prediction_count=0`；invalid prediction 以空预测计分。
- Generation 的 seen/unseen schema-valid 数分别为 matched 771/1,834（原 seen 778）、null 778/1,839、within-scene 759/1,821（seen denominator 769）、global 766/1,828（原 seen 778）；所有 job 的 `oom_split_retries=0`。
- Login node `~/run` 所在 JuiceFS 为 300 G、已用 246 G、剩余 55 G（82%）。

Training artifact 为 `r2r-rxr-oracle-t0-v1-s2-e2-seed42`：

- R2R+RxR discovered/loaded/retained 为 30,815/30,740/30,511；
- missing cache 75，token budget 丢弃 229（0.745%，低于 2% guard）；
- 2 epochs 共 7,628 optimizer steps，最终 train loss 0.170095；
- training manifest 共 30,512 行（header + 30,511 retained examples）。

## Artifact 复制与完整性

已复制到本机：

| 内容 | 本地路径 |
|---|---|
| final LoRA adapter、tokenizer、training metrics/manifest/prompt | `outputs/llm_grid/r2r-rxr-oracle-t0-v1-s2-e2-seed42/` |
| remote 原始四条件评价 | `outputs/llm_grid_eval/oracle-t0-v1-e2-{matched,null,within-scene,global}/` |
| 本地 common-population 重评 | `outputs/llm_grid_eval/oracle-t0-v1-e2-{matched,null,within-scene,global}-common/` |
| 三组 paired bootstrap | `outputs/llm_grid_eval/bootstrap-{null,within-scene,global}-vs-matched-common/` |
| 完整 prediction caches | `data/llm_navigation/llm-grid-oracle-t0-v1-e2-{matched,null,within-scene,global}/` |
| oracle evidence/index/manifest | `data/llm_grid_oracle_evidence/` |
| 四个成功 Slurm logs | `outputs/slurm_logs/oracle-t0-v1-e2/` |

传输使用 tar.gz；因一个旧 `scp` writer 与新 writer 同时写同一临时文件，第一次大包虽能通过 `gzip -t`，但 SHA-256 不匹配，未用于提取。之后把 remote 已验证 archive 切为 16 个独立 chunks，逐块 hash 对齐后重组：

- 完整 archive：330,223,146 bytes；
- archive SHA-256：`0ec672985416ef9aff57742b900f6d55e3ab3c0c0ce821be34e83b97c37637fe`；
- final `adapter_model.safetensors`：335,604,696 bytes；
- adapter SHA-256：`c93adf4a4df9fa3fa28b7643dfaef5dd6e2a4c91a743e17a99dece1553976def`。

Remote epoch 2 与 `final` adapter hash 完全相同，因此只复制 `final`，没有浪费约 336 MB 保存 duplicate checkpoint。Epoch 1 也未复制，因为本轮不做 checkpoint curve。

完成本地 hash、denominator 与 artifact presence 验证后，已按批准执行清理：

- 本机删除 transfer archives、16 个 chunks、corrupted retry 与 staging extraction，保留上述唯一一套 decompressed canonical artifacts；
- login node 删除本轮 seed-42 training output、四条件 prediction caches/eval outputs、四个已复制日志与 `/tmp` transfer staging；
- login node **保留** `data/llm_grid_oracle_evidence`（约 67 MiB），因为后续 paired training seeds 会复用其 evidence 与 hash-pinned manifests，删除只会导致重新生成或回传。

清理后逐路径确认所有 delete targets 均不存在，remote evidence root 与本地 adapter/common eval/bootstrap/logs 均仍存在；本地 adapter SHA-256 再次验证一致。

## Common population 修正

Within-scene assignment 在 `val_seen` 的三个 singleton scenes 上无法定义 non-self donor，因此显式排除 9/778 条；`val_unseen` 无排除。共同集合由 within-scene population manifest 固定：

- `val_seen`：769 examples、50 scenes、9 exclusions；
- `val_unseen`：1,839 examples、11 scenes、0 exclusions。

本地四条件重评均得到上述 denominator、`missing_prediction_count=0`，并通过 evidence key/manifest hash 检查。相关 contract tests 为 23 passed。

原始 matched `val_seen` F1/IoU 为 0.4902/0.3590，修正后为 0.4900/0.3581；global 为 0.0652/0.0367 → 0.0650/0.0365。修正对结论没有实际影响，但后续 launcher 必须让四个 condition 都显式使用同一 population。

## `val_unseen` 主要结果

| Condition | Overall F1 | Overall IoU | Unobs P | Unobs R | Unobs F1 | Unobs IoU | Pred∪evidence F1/IoU |
|---|---:|---:|---:|---:|---:|---:|---:|
| matched | **0.3175** | **0.1954** | **0.1162** | 0.1336 | **0.1243** | **0.0663** | 0.2727 / 0.1579 |
| null | 0.0000 | 0.0000 | 0.0000 | 0.0000 | 0.0000 | 0.0000 | 0.1975 / 0.1095 |
| within-scene | 0.1993 | 0.1205 | 0.0658 | **0.1690** | 0.0947 | 0.0497 | 0.2245 / 0.1264 |
| global | 0.0952 | 0.0549 | 0.0222 | 0.0729 | 0.0341 | 0.0173 | 0.1758 / 0.0964 |

Matched 相对 null/within/global：

- Overall F1：+0.3175 / +0.1182 / +0.2223；
- Overall IoU：+0.1954 / +0.0749 / +0.1405；
- Unobserved F1：+0.1243 / +0.0297 / +0.0903；
- Unobserved IoU：+0.0663 / +0.0166 / +0.0490。

Interpretation：

- Null 输出为有效 empty prediction，因此其 union 等于 evidence-only baseline（F1/IoU 0.1975/0.1095）。Null 是强烈的 input-format distribution shift，主要用于确认模型不能在无 evidence 时工作；within/global shuffle 是更有信息量的 controls。
- Within-scene 的 unobserved recall 高于 matched，但 precision 只有 matched 的约 57%；matched 的优势主要是 instruction 与当前观测匹配后减少 false positives。
- Global 仍有非零性能，说明 instruction 和非匹配 semantic inventory 仍提供 generic category/layout prior；matched 对它的大幅优势才体现 observation identity 的价值。
- Matched output 还超过同一 prediction 的 deterministic `prediction∪evidence` 后处理：F1 +0.0448、IoU +0.0375。直接 union 会加入大量与 route target 无关的可见语义 false positives；LLM 的筛选优于无条件复制。

## Paired scene-cluster bootstrap

使用 10,000 repetitions、seed 42；positive delta 表示 matched 更好。95% percentile CI：

| Baseline | `val_unseen` F1 delta [95% CI] | IoU delta [95% CI] |
|---|---:|---:|
| null | +0.3175 [0.2913, 0.3491] | +0.1954 [0.1764, 0.2184] |
| within-scene | +0.1182 [0.0863, 0.1473] | +0.0749 [0.0565, 0.0911] |
| global | +0.2223 [0.1894, 0.2615] | +0.1405 [0.1193, 0.1667] |

Matched 相对 within-scene 的 object/region category F1 delta 也为正：+0.0424 `[0.0208, 0.0643]`、+0.0679 `[0.0285, 0.1092]`。Direction cosine delta 为 +0.0540，但 CI `[-0.0019, 0.1225]` 穿过 0，不报告可靠 direction improvement。`bootstrap_probability_positive` 不是 p-value，本轮只按 CI 是否跨 0 解读。

<details>
<summary>指标语义与仍未完成的统计验证</summary>

- Overall Cell F1/Raster IoU 是 episode-macro；observed/unobserved、evidence/union、mentioned/unmentioned 与 category presence 是 pooled counts，不能直接相减或互相加权还原。
- Cell count 是 category channel × grid cell，不是 unique 2-D locations。
- Evidence baseline 只使用实际写入 prompt 的 semantic grid；`structure`、`other`、`free-space` 已排除，free/observed geometry 不被偷算作 semantic prediction。
- Recipient evidence 始终用于 observed/unobserved partition 和 deterministic baseline，即使 prediction 来自 shuffled donor condition；这是共同 causal frame。
- Diagnostics 的分布统计使用全部 expected episodes，correlation 只使用 schema-valid predictions。
- 当前 evaluator 验证 evidence key/manifest hash，但不验证 assignment kind/seed/hash；本轮已人工检查四组 cache manifests 均固定同一 checkpoint、evidence manifests、seed 42、scale 2、3,072/4,096 token limits 与相同 system-prompt hash。
- 现有 bootstrap 不含 pooled unobserved 与 union sufficient statistics，因此不能给这两个预设 completion criteria 正式 CI。

</details>

## RGB-D perception 决策状态

07/24 已完成 segmentation 与 detection 的候选、理由和论文证据记录；本日结果没有改变该表示决策：

1. 主路径仍是 closed-vocabulary、efficient RGB-D semantic segmentation；
2. detector-generated instance masks 是 latency fallback；
3. raw boxes 只作速度上界与 box-to-grid distortion ablation；
4. object segmentation 与 panoramic region classification 分开；
5. DFormer/DFormerv2、ESANet、YOLOE masks 与 YOLO-World boxes 的具体选择继续留待 paired local benchmark。

Oracle screen 的成功说明“起点可见语义是否值得做”已有正向证据，但还不能说明哪个 deployable perception model 最合适，也不能把 GT semantic 上界误写成 RGB-D 系统结果。

## 决策与下一步

1. 暂不启动 Try5 navigation，也不把单 seed 结果写成最终 causal claim。
2. 修正 remote launcher，使 matched/null/within/global 都显式使用同一个 hash-pinned common population。
3. 扩展 paired scene-cluster bootstrap，加入 pooled unobserved 与 condition-specific union sufficient statistics，并明确 legacy-union baseline contract。
4. 用至少三个 paired training seeds 复现 matched 与 controls；重点报告 unobserved precision/recall，而不是只报告 F1。
5. 保留本轮成功与失败 artifacts；原 within-scene failure 作为 strict exclusion design 的消融证据。
6. 等 paired-seed predictor 结论后，再决定 DFormer/DFormerv2、ESANet、YOLOE masks 或 detector box ablation 的实际 benchmark 顺序。

## LLM-Grid 合理性检查

本检查只读取已有的 R2R `val_seen`/`val_unseen` LLM-Grid cache 与 evaluator records；没有启动训练、预测生成或 navigation cache generation。固定比较 epoch 1、2、5、10；每个 epoch 都严格保留 `val_seen=778`、`val_unseen=1,839` 个 expected episodes（histogram 总数也分别相等）。IoU 与 category 指标中的无效输出仍留在分母并按 evaluator 规则计分。

### 定量结果

下表为 episode-macro category-presence F1（object / region / combined）：

| Source | `val_seen` | `val_unseen` |
|---|---|---|
| epoch 1 | 0.6229 / 0.6548 / 0.6299 | 0.6444 / 0.6377 / 0.6407 |
| epoch 2 | 0.6805 / 0.7316 / 0.6921 | 0.6747 / 0.6612 / 0.6705 |
| epoch 5 | 0.6036 / 0.6377 / 0.6117 | 0.5824 / 0.5843 / 0.5824 |
| epoch 10 | **0.7201 / 0.7500 / 0.7277** | 0.6552 / 0.6402 / 0.6513 |
| predict-all-category baseline | 0.5308 / 0.5150 / 0.5289 | 0.5599 / 0.5090 / 0.5482 |

- 四个 model epochs 的 combined F1 都高于 predict-all baseline；epoch 10 的 seen 最高，但 unseen 的最高值是 epoch 2（0.6705），故不能把 seen 后期改善写成 unseen 单调泛化改善。
- tolerant IoU 分布总体集中在低值。`IoU >= 0.10 / 0.25 / 0.50` 在 seen 上分别为 epoch 1 `0.4550/0.1015/0.0347`、epoch 2 `0.5977/0.2147/0.0784`、epoch 5 `0.5746/0.2545/0.1350`、epoch 10 `0.6658/0.3239/0.2082`；unseen 分别为 epoch 1 `0.4334/0.0440/0.0000`、epoch 2 `0.6096/0.1136/0.0000`、epoch 5 `0.5389/0.0875/0.0022`、epoch 10 `0.6107/0.0788/0.0000`。因此 seen 的 epoch 10 高-IoU tail 明显，但 unseen 在 `0.50` 没有稳定 tail。
- full-population schema-invalid count（seen/unseen）分别为 epoch 1 `41/59`、epoch 2 `8/9`、epoch 5 `135/252`、epoch 10 `10/6`，均与 evaluator `episodes.csv` 一致；epoch 5 的异常率是该轮 F1 与 IoU 退化的重要解释，不能只看少数高 IoU seen episodes。
- 预测 object category mean（seen/unseen）随 epoch 为 epoch 1 `9.847/10.052`、epoch 2 `9.398/9.234`、epoch 5 `9.387/9.705`、epoch 10 `10.386/10.340`；region 为 `3.265/3.254`、`3.413/3.421`、`3.107/3.308`、`3.599/3.619`。epoch 10 预测类别数略多，尤其 region；这与其 seen F1 提升一致，但不构成 unseen spatial accuracy 的证据。

### 图与定性检查

- [IoU histogram](../images/llm_grid_r2r_rxr_sanity/iou_histograms.png)、[IoU survival](../images/llm_grid_r2r_rxr_sanity/iou_survival.png)、[category F1](../images/llm_grid_r2r_rxr_sanity/category_f1.png)、[object category counts](../images/llm_grid_r2r_rxr_sanity/object_category_counts.png) 与 [region category counts](../images/llm_grid_r2r_rxr_sanity/region_category_counts.png) 均已逐图检查；标签、legend、轴和 panel 均可读。
- 17 个共同 `val_unseen` examples 的 [comparison-sheet directory](../images/llm_grid_r2r_rxr_sanity/comparisons/) 覆盖 8 个 scene；已逐项检查 `2azQ1b91cZZ/R2R_val_unseen_1756`、`EU6Fwq7SyZv/R2R_val_unseen_1275`、`QUCTc6BB5sX/R2R_val_unseen_176`、`TbHJrupSAjP/R2R_val_unseen_1409` 四个不同 scene。epoch 1 常见稀疏或漏掉 GT support；epoch 2/10 在若干 sample 中恢复更多 object/region support，但仍有坐标偏移和额外 category；选入 sheet 的 epoch 5 predictions 均为 schema-valid，但仍可见过宽/不匹配的定性布局，这与 full-population schema-invalid 计数是两个独立问题。定性图支持“category presence 有信息”，但不能替代 IoU 或 navigation 评价。
- 初版 sheet 把 5 个约 2.8–3.6k 像素宽的图横向拼成约 17.9k×2.1k，viewer 中 legend/label 不可读；已改为两列三行，并以 targeted regression test 固定五图输出尺寸。重跑后 sheet 约 7.2k×6.3k，四个检查 scene 都没有 clipped label、不可读 legend 或空白 panel。

### R2R-only 对照补跑

恢复的 `data/llm_navigation/llm-grid-r2r-legacy-r1p5-direction5-scale2`
cache 只包含 final checkpoint 的部分 `val_seen`/`val_unseen` raw
predictions，且 generation budget 为 2,048 tokens，不能直接作为完整
checkpoint curve。进一步审计发现，R2R-only checkpoints 使用历史 prompt
contract：user prompt 包含 `dataset R2R` 与 `direction x/z`，system prompt
SHA-256 为 `6de023cedc1409ce82c5d93d52083b8b980f788b380ea475c69451370cf28409`；
当前代码的 tag-free、`start direction right/up` prompt 与当前 system prompt
均不兼容。因此不能在恢复的 namespace 中直接续跑，否则会混合 prompt
contract 与 decode budget。

已在 `820c959` 加入显式 `r2r-legacy-v1` cache-generation contract，并为
epoch 1–10 建立全新的 cache keys。所有 epoch 固定使用历史输入/system
prompt、相同的 1,024-token input budget，以及与 mixed sweep 相同的
4,096-token decode budget。Restored partial cache 只保留作历史定性
provenance，不进入 formal comparison。

- Login node 已同步并 hash 验证 epoch 1–10 checkpoints；transfer archive
  SHA-256 为
  `d61536c677fdb89d2b065954228b2e3cec1d24b37f327fc40c31dc82b6298f5b`，
  解压后确认 10 个 adapter 均存在，transfer archives/chunks 已清理。
- Cache Slurm array 为 `1188486`：10 个 task、每个 epoch 1 GPU，生成完整
  R2R `val_seen`/`val_unseen` predictor-eval cache；提交后 10 个 task
  均进入 RUNNING，早期日志无 traceback、prompt assertion、missing file
  或 OOM。
- Strict dependent evaluator 为 `1188496`，依赖
  `afterok:1188486_*`，输出到
  `outputs/llm_grid_eval/r2r-only-checkpoint-sweep-r1p5`；任一 epoch
  缺 raw prediction 时会显式失败。
- 参考 mixed 10-epoch sweep 的 13:02:32 runtime，本次 10 个单 GPU task
  并行预计约 11–13 小时。结果完成前，07/24 的 R2R-only 对照 checkbox
  保持未勾选，也不提前判断更多数据是否改善 overfitting。
