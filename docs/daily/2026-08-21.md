# 2026-08-21

## 路线门控认知地图更新

- 在 `exp/pose-gated-cognitive-map` 实现方案 12：保留 Try5 的全节点单向地图读取，仅在 Map Encoder 前更新 `Pt`。
- 新增 v2 空间语义缓存生成器，标签为 `12×37×5` 与深度覆盖；原 v1 缓存不兼容。
- 预训练使用 expert prefix、同场景离路线负例，以及 `L_visual/L_route/L_seen/L_fix/L_keep`；DAgger 冻结 SpatialVisualHead，仅更新路线门和后验修正器。
- DAgger 读取实时 pose 与 `reference_path`，路线门关闭时保持地图状态。
- 修正预训练 GT 配对：按 `dataset/split/episode_id` 读取 `data/cognitive_maps`，并修正本机 `torchrun` 参数拼写。

## 未验证项

- 尚未生成 v2 缓存，也未开始完整预训练或 DAgger。
- 需确认目标 GT namespace 与新预训练 checkpoint 路径后启动训练。
