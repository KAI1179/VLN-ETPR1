# 2026-08-06

## 讨论

- [ ] **进行中**：LLM-Grid-Oracle-t0 接入 VLN（从 pretraining 开始）
    - 回答的问题：不及预期的原因，是利用了偏差较大的预训练检查点，还是 evidence 起到了反作用
    - ordinary/oracle 两臂 matched pretraining 已启动并持续运行；作业辨识与 checkpoint retention
      见 [08-07 matched pretrain 记录](2026-08-07.md#matched-pretrain-双作业辨识修复)及
      [paired retention](2026-08-07.md#paired-pretrain-checkpoint-retention)。完整 VLN pipeline 尚未结束，
      因此不能标记 completed。
- ~~LLM-Grid 3：添加了 object evidence 的网格预测~~
    - Segmenter: YOLOE
    - 使用 YOLOE object evidence，让 LLM 预测 object + region grid
    - 接入 VLN 查看效果（预训练 - Dagger - GRPO），可以添加消融对比：
        - no evidence
        - YOLOE evidence
        - GT semantics (all categories)
        - GT semantics (YOLOE categories)
    - 回答的问题：稀疏、相对精确但是低召回率的 object evidence 是否能提升网格的预测或导航任务？
    - YOLOE matched chain 已暂停；OOOPS 的 bounded screen 与 OpenScene/Mosaic3D permission gate 已闭合，
      三候选最终均为 NO-GO，见
      [08-07 三候选最终决策](2026-08-07.md#j-ooops-frozen-scientific-screen-与三候选最终决策)。
- [x] **已完成**：MP3D-tested segmenter 的 bounded candidate 搜索与三候选执行。
    - SFSS、360BEV、OOOPS、OpenScene、Mosaic3D 等 artifact/contract/quality 结论统一记录在
      [08-07 MP3D segmenter sections](2026-08-07.md#sfss-mmsi-artifact-audit-与-bounded-screen-checklist)。
    - “已完成”只表示本轮冻结 search space 与三个获授权候选已有终止决策，不表示已找到新的 GO candidate。
- [x] **已完成**：HM3DSem 定向调研。
    - 目标从“再找一个 checkpoint”改为审计独立 training/development supervision、exact-23 taxonomy
      coverage 与 cross-domain protocol；完整 checklist 与结论见
      [08-07 HM3DSem checklist](2026-08-07.md#hm3dsem-定向调研已委派)。
    - native HM3DSem v0.2 canonical rendering 作为 object-only training/development supervision
      **GO**；作为 reusable standalone segmenter checkpoint source **NO-GO**；heuristic room proposals
      作为 region supervision **NO-GO**。
- [x] **已完成**：LLM finetuning curriculum learning 契约与 CPU audit；待后续 GPU 实验。
    - “多条指令”指同一 scene 的多条 English instructions，而非同一路径的同义指令；目标是假设同一环境中
      不同路线的描述可提供互补环境信息，使 LLM 更准确地预测当前样本的 cognitive map。
    - 初始 finetuning 阶段给定同 scene 多条指令，随后逐渐减小上下文指令数，最终与推理 contract 一致只输入
      当前样本的一条指令。
    - 实现前先做只读 corpus/objective/leakage audit：冻结当前指令与辅助指令的采样规则、目标仍由哪个 episode
      定义、是否允许同 scene 的其他 split/route 信息，以及 curriculum 造成的 train–inference mismatch；不能把
      `val_seen`/`val_unseen` 指令或 target 信息引入训练。
    - 初步 corpus/contract audit：R2R train 为 `10,819` 条 English examples / `61` scenes，RxR English train
      为 `19,996` / `59`，且 RxR 的 59 个 scenes 均与 R2R 重叠；单 scene 最多有 `969` 条 combined
      instructions，因此不能把全 scene 文本直接塞入 prompt。当前 target、`direction_vectors` 与 mention status
      都属于 primary episode，辅助指令必须明确标记为 context，不能 union targets。
    - 最小可检验方案暂定为固定 train-only、同 scene、不同 start metadata 的 deterministic auxiliary list，按
      `K=3→2→1` 退火；所有 checkpoints 仍用原始 `K=1` validation contract。实现前先做 CPU-only 的
      K=1/2/3 token-retention 与 common-cohort audit，若 K=3 丢弃过多则退为 `2→1`，不为此扩大预算。
    - Idea：利用同 scene 互补描述做先易后难；是否真的更“容易”及是否改善 unseen-scene 泛化仍需验证，不能
      预设为成立。
    - 全量 CPU audit 显示 PRIMARY `K=1/2/3` 保留率为 `98.95%/98.91%/98.43%`，因此
      matched `K=1` 对比 `K=3→2→1` 在当前预算下可行。仅按 scene 选 context 的 cohort 有
      `49.04%` 跨楼层；target-free start-height 分组在 train audit 中覆盖 `87.53%`、未观察到跨层。
    - no-PRIMARY union 只有 `66.51%` cohort 同时满足预算，且多轨迹 directions 与当前 schema 不兼容；
      本轮决定不接入生产训练。详见
      [08-07 课程学习审计](2026-08-07.md#同场景多指令课程学习审计)。
- [x] **已完成**：计算 Oracle evidence 加入后 LLM 预测的 IoU。
    - matched/null/within-scene/global causal screen 与 common-population 结果见
      [07-25 起点 oracle evidence 因果 screen](2026-07-25.md#val_unseen-主要结果)。
- [x] **已完成**：LLM 预测后处理（筛选/合并）审计。
    - exact duplicate 已由 parser 去重；输出没有可校准的 category/cell confidence。
      mentioned-only、singleton/category support 与同 channel 邻域过滤都未通过冻结的
      `val_seen` 多指标门槛，直接 `prediction ∪ evidence` 也因 precision 下降而变差。
      结论为 no-code/no-go，见 [08-07 后处理审计](2026-08-07.md#llm-grid-后处理审计)。
- [x] **已完成**：原始 ETP-R1 pipeline 中 GRPO 的设计与收益诊断。
    - 先解释作者为何在 GRPO 冻结大部分 policy、只训练四个 navigation modules，以及 GRPO 相对 DAgger
      是否改变 observation/model input、可训练参数、loss/reward 或 rollout distribution；已有代码事实摘要见
      [微调阶段可训练/冻结的模块](../NOTE.md#微调阶段可训练冻结的模块)。
    - 再解释为何 GRPO 在本项目 pipeline 中提供的增益较小：需要区分 cognitive-map path 没有随 GRPO
      适配、输入或 checkpoint contract 改变、reward/optimization mismatch，以及 upstream DAgger 已吸收
      主要收益等竞争假设，不能预设一定是 cognitive map 冲突。
    - `exp/grpo-map-adaptation-ablation` commit `c3046b7` 只回答“让 `graph_map_attention` 在 GRPO 中
      trainable 是否有帮助”；它是相关 ablation，**没有回答**作者原始 GRPO 决策与本项目低增益的原因，
      因此不把该历史对比写成因果结论。
    - 诊断确认原论文只微调 DPFT + SAP，但没有 full-policy causal ablation；本项
      cognitive-map GRPO 又重放 detached map tokens，单纯解冻 map encoder 不会形成梯度路径。
      现有结果更支持 GRPO 改善效率/停止行为，而非新的 success headroom；详见
      [08-07 GRPO 契约诊断](2026-08-07.md#grpo-与认知地图契约诊断)。
- [x] **已完成**：改善同一 cognitive map 内多个语义类别色块的重叠可视化。
    - 第一优先级是让 category layers 的重叠数量、组成和空间范围在日常检查与展示中可读，不再依赖当前有顺序的
      alpha blending 猜测重叠关系。
    - 初步 audit 确认重叠不是边缘情况：抽查 200 个本地 maps 时，occupied cells 中发生多类别重叠的中位比例为
      GT/predicted objects `85.9%/94.1%`，GT/predicted regions `27.3%/35.9%`，observed 最大 object
      overlap depth 为 12。当前 painter-order alpha compositing 会隐藏共存类别并产生与绘制顺序相关的颜色。
    - 第一实现提议为 order-invariant per-cell categorical mosaic：每个 cell 的子块只表达 co-location，不表达物理
      面积；极密或缩放后不可读时另输出 overlap-count diagnostic 与 top combinations，而非退回 dominant-category
      或混色。先在 `feat/grid-map-overlap-visualization` 做 reusable renderer 与 July presentation path 的统一。
    - prediction–target 的 TP/FP/FN 对照为 capacity 允许时的 nice-to-have，不阻塞第一优先级。
    - 共享 renderer 已改为 order-invariant per-cell category mosaic，July presentation 路径复用同一原语；
      见 [08-07 重叠可视化](2026-08-07.md#认知地图重叠可视化)。
- [x] **已完成**：设计并实现 category-level 相对布局指标。
    - 目标是补充 raster IoU：即使绝对栅格未对齐，也应识别预测是否保留类似“table#1 位于 bed#1 西侧”的
      instance-level 相对布局。
    - GT 可从 bounding boxes 得到 object/region instances；预测是 category-first grid，无法可靠恢复 instances，
      因此不能用 connected components 等 heuristic 假装得到一一对应的预测实例。
    - 需比较 world-frame `west/east/north/south` 与基于 agent start orientation 的 `left/right/front/behind`；后者
      会引入 start-relative frame，而当前 LLM target 遵循 ADR 0001 的 level-local coordinates。
    - 初步设计结论：MP3D level-local axes 不是地理方位，主指标不应称 `west/east`。先评价每对 category
      occupancy 的相对位移：coverage、centroid displacement direction/distance；若多实例导致 centroid cancellation，
      再比较 pairwise offset cross-correlation distribution。该表示是在 instance IDs 被栅格合并后仍可识别的
      category-level quotient，不声称恢复 `table#1`；start-frame `left/right/front/behind` 只作为 secondary view。
    - 真正的 instance-to-instance graph metric 需要 LLM-Boxes 或新的 instance-native prediction contract；利用 GT
      instance count/centres 对 predicted grid 做伪分割只能标为 oracle-assisted probe，不能作为 headline metric。
    - 先提出可计算且明确承认 instance ambiguity 的表示、matching 与 invariance 方案；若无法形成有意义的
      instance-level 指标，则保留 category-distribution 或 pairwise-layout 诊断，不为完成 checklist 强行实现。
    - train corpus 中 object/region category-example 多实例率为 `67.71%/44.12%`，centroid gate 失败。
      实现改用 category-pair relative-offset distributions，分开报告 pair coverage 与 conditional
      Sørensen similarity；共同平移不变且不伪造 instances。见
      [08-07 相对布局指标](2026-08-07.md#llm-grid-相对布局指标)。
- [x] **已完成**：predict-all category F1 在 `val_unseen` 上为 0.5482，见 [07-25 定量结果](2026-07-25.md#定量结果)。

## YOLOE-t0 × Oracle predictor 推测性并行 cache 预注册

### 目的与解释边界

为避免本地 8×RTX 3090 空闲期间等待远端 Oracle cache，本轮按用户决定并行准备
YOLOE `pretrain/mixed` evidence 与 navigation cache。该运行明确标记为
**YOLOE evidence × Oracle-conditioned predictor 的 distribution-shift、cache-only
ablation**，不是已通过 P8 quality gate 的 deployable candidate，也不是
YOLOE-conditioned predictor。P8 的正式结论仍为 NO-GO；本轮结果不能单独推翻该
结论，也不能归因区分 perception noise 与 predictor distribution shift。

固定输入与命名如下：

- YOLOE source revision：`40cd606cabdbe2b566d6f14a6b162c89206e9a1b`；
- YOLOE checkpoint SHA-256：
  `14e9c146f7f7efa0bfea8c5f82952c9f95fb1f9c3bf0f18b3727b76624f9d746`；
- MobileCLIP-B weight SHA-256：
  `670844f7a886dd6eff7a9285adfc53f3d3c889c03bfc8354010cb5c6bf27441a`；
- evidence key：`yoloe-t0-pretrain-mixed-v1`；
- resumable work root：
  `data/llm_grid_yoloe_work/yoloe-t0-pretrain-mixed-v1/`；
- strict evidence root：`data/llm_grid_oracle_evidence/`；该路径名沿用现有
  `GridEvidence` storage contract，不表示 YOLOE 是 oracle；
- predictor：
  `outputs/llm_grid/r2r-rxr-oracle-t0-v1-s2-e2-seed42/checkpoints/final`；
- navigation cache key：
  `llm-grid-yoloe-t0-v1-oraclepred-e2-pseed42-matched-r1p5-d5-s2-pretrain-ablation-v1`；
- scope / generation：`pretrain-only`、batch size 4、input 3072、completion
  4096、8 workers、matched evidence assignment、seed 42。

当前输入扫描的预期 population 为 47,416 个 English `pretrain/mixed` records，按
完整 start observation identity 去重后为 13,964 个 observations、72 个 scenes。
这些数值只用于启动前 guard；最终 manifest 必须从实际输入重新计算并严格验证，不能
靠本记录补齐或容忍缺失。

### 本地执行链与失败语义

新增本地 launcher：
`scripts/local/llm-grid-yoloe-t0-pretrain-cache.sh`。链路固定为：

1. `etpr1-uv` 中启动 8 个 deterministic shards，分别限制到一个 GPU UUID，渲染
   t=0 RGB/depth/pose raw artifacts；YOLOE inference 不得读取 oracle semantic、
   instruction、goal 或 trajectory；
2. 前一阶段全成功后，在 pinned YOLOE `.venv` 中启动 8 个 single-UUID workers，
   使用冻结的 text prompts/mask protocol 生成并投影 YOLOE object evidence；
3. 所有 inference shards 成功后，由 `etpr1-uv` finalizer 验证 exact population、
   shard commitments 与 artifacts，再原子发布 evidence index/manifest；
4. 只有 finalizer 成功，才使用上述 Oracle-conditioned predictor 启动 batch-4
   LLM navigation cache generation。

render 与 inference artifacts 均按确定性 shard 可恢复：重跑时必须 hash/schema
验证已有文件后跳过，不能静默覆盖不一致 artifact。Launcher 使用单实例 file lock、
逐阶段独立时间戳日志和 `wait -n`；任一 child 非零退出会立即终止同阶段其余 children，
且不会进入下一阶段。YOLOE frozen environment 不含 Habitat，因此 render 与 inference
必须保持进程/环境隔离，禁止把主环境 site-packages 注入 YOLOE `.venv`。

### Bounded smoke 与 full run 启动

真实单 observation smoke 依次执行 RGB-D render、pinned YOLOE inference、multi-label
ego/target projection、strict index publication、index reload 与 finalized-index resume，
最终 evidence 含 3 个 sibling examples、1 个 observation。该 smoke 发现并修正两个
unit tests 未覆盖的 integration bug：Habitat 0.1.7 模板名应为 `RGB_SENSOR` 而非
`COLOR_SENSOR`；worker provenance 实际发布在 `masks/pretrain/`，finalizer 原先读取了
错误层级。另修正 non-interactive Conda activation 在 `set -u` 下访问未绑定 Qt 环境
变量的问题。修正后 focused 9 tests、Ruff、ty、`bash -n` 与重复 finalization 均通过。

full resumable chain 于 `2026-08-06 03:37:22 UTC`（11:37:22 CST）启动，本地
supervisor PID 为 `932818`，8 个 render shards 为 `932843`–`932850`，日志前缀为
`data/logs/yoloe-t0-pretrain-mixed-v1/20260806T033722Z-*`。启动后约 2 分 18 秒完成
389/13,964 个 raw observations、写入约 1.2 GiB；早期线性外推 render 约需 1.4–1.8
小时，YOLOE inference/finalize 预计再需约 0.5–1 小时。随后自动进入 batch-4 LLM
navigation cache；结合远端同模型实测，整条 chain 的保守 remaining ETA 为约
37–52 小时。该 ETA 仍以 LLM generation 为主，会在进入 navigation-cache stage 后
用实际各 worker tqdm 重新估计。

## 改为 YOLOE-conditioned predictor 全匹配链路

用户确认主实验不再采用 YOLOE evidence × Oracle-conditioned predictor 的
distribution-shift cache，而改为完整匹配链路：YOLOE t=0 evidence → 从同一 base model
独立训练的 YOLOE-conditioned predictor → `pretrain/mixed` navigation cache → navigation
pretraining。Oracle-conditioned adapter 不作为新 predictor 的初始化，以免把 Oracle
conditioning 混入主实验；原 speculative cache 只保留为可选诊断，不占用当前关键路径。

现有 predictor trainer 固定读取 `R2R/train` 与 English `RxR/train` evidence index，不能
直接读取 `pretrain/mixed` index。Oracle predictor 的 production run 保留 30,511 个
examples、训练约 8 小时 46 分。检查还发现，当前 13,964 个 pretraining observations 与
原始 VLN-CE train observations 的 exact observation IDs 没有交集，因此不能未经明确
pose-equivalence 验证就把 artifacts 静默重索引。下一步应为两个 train namespaces 生成
冻结 YOLOE evidence，然后按 Oracle run 的相同 corpus、target namespace、seed、LoRA 与
两轮训练设置，从同一 base model 训练 fresh adapter。

为避免 launcher 自动进入预计 35–50 小时的错误 cache，`2026-08-06 04:02 UTC` 向旧
supervisor PID `932818` 发送 TERM；其 trap 正常结束 8 个 workers，保留并可严格恢复
4,221/13,964 个 raw observations。随后移除 launcher 的 Oracle predictor/cache stage，
保留 render → frozen YOLOE inference → strict finalization，并以 detached screen session
`yoloe-pretrain-evidence` 于 `2026-08-06 04:03:10 UTC` 恢复执行。screen 主日志为
`data/logs/yoloe-t0-pretrain-mixed-v1/screen-evidence.log`，本轮 shard 日志前缀为
`20260806T040312Z-*`。恢复初期 8 workers 正在逐个 hash/schema 验证已有 artifacts，
因此 count 暂时不增长；`/proc/<pid>/io` 显示所有 workers 持续读取，并非停滞。

### 全匹配 continuation queue

为使本地长链在无人值守时按依赖顺序继续，新增 fail-fast continuation launcher：
`scripts/local/llm-grid-yoloe-matched-chain.sh`。它等待上述 `pretrain/mixed`
evidence manifest 原子发布；若 screen 提前退出且 manifest 不存在则立即失败，不会无限
等待。后续固定顺序为：

1. 生成 `yoloe-t0-train-v1` 的 English `R2R/train` 与 `RxR/train` evidence；
2. 从相同 base model 独立训练两轮 YOLOE-conditioned LoRA，输出
   `outputs/llm_grid/r2r-rxr-yoloe-t0-v1-s2-e2-seed42`；
3. 用该 adapter 与 `yoloe-t0-pretrain-mixed-v1` evidence 生成 matched navigation cache
   `llm-grid-yoloe-t0-v1-yoloepred-e2-pseed42-matched-r1p5-d5-s2-pretrain-v1`；
4. 与 ordinary cache 冻结 navigation-ready common intersection，并固定 artifact SHA-256；
5. 用 4 GPUs、相同 baseline checkpoint 启动 Try5 + LLM-Grid navigation pretraining。

trainer 的真实固定 population 为 R2R 10,819 examples / 2,564 unique observations 与
English RxR 19,996 examples / 6,668 unique observations，共需新增 9,232 个 YOLOE
observations。为支持该 population，将原先只接受 `pretrain/mixed` 的 bridge 泛化为显式
dataset/split namespace，同时保留正在运行的 legacy pretrain render-completion 路径，
保证本轮 finalizer 能读取已经写出的 shard commitments。共享 cohort 的 CLI 与 pretrain
参数由 Oracle-specific `ordinary/oracle` 命名改为通用 `reference/candidate`，避免把
YOLOE candidate 错标为 Oracle。

检查结果：两个 local launchers 与受影响 submit scripts 均通过 `bash -n`；相关 Python
文件通过 Ruff 与 ty；focused 42 tests 通过。所有新输出在 queue 启动前均不存在，ordinary
47,416-record cache manifest 与 baseline `model_step_367500.pt` 均已验证存在。

continuation 于 `2026-08-06 04:27:35 UTC` 以 detached screen
`yoloe-matched-chain` 启动，shell PID `949920`，主日志为
`data/logs/yoloe-t0-matched-chain/screen-chain.log`。启动后确认其 child 为 30 秒 bounded
poll，并显示正在等待 pretrain evidence manifest。同期当前 render 为 8,081/13,964；30 秒
增量 93，即约 3.1 observations/s，按该瞬时速率 render 剩余约 32 分钟。

## 暂停 YOLOE 下游链并扩大 MP3D segmenter 搜索空间

### 搜索偏差与当前决策

此前候选发现主要从通用 RGB-D / open-vocabulary segmenter 出发，再检查其能否用于
Matterport3D；只针对 ESANet 单独检索过 Matterport3D 证据，没有系统地从“已在 MP3D
训练或测试的 segmenter”反向搜索。这构成真实的 search-space bias。结合 YOLOE P8
已预注册为 NO-GO，本轮决定先暂停 matched LoRA、navigation cache 与 navigation
pretraining，不再让完整链路自动消耗数日 GPU 时间；但保留 YOLOE artifacts 作为
open-vocabulary baseline，不能因搜索偏差直接断言 MP3D-specific 模型一定更好。

`2026-08-06 21:13 CST` 向 detached screen `yoloe-matched-chain` 发送 Ctrl-C；launcher
及其 evidence workers 均正常退出，screen 已消失。暂停时状态为：

- `yoloe-t0-pretrain-mixed-v1/pretrain/mixed` strict manifest 已发布；
- `yoloe-t0-train-v1/R2R/train` strict manifest 已发布；
- `RxR/train` 的 raw 与 mask 均为 6,668/6,668，但 strict evidence manifest 尚未发布；
- fresh YOLOE-conditioned LoRA、matched navigation cache 与 navigation pretraining
  均未启动；已有 raw/mask 可由 launcher 严格验证后恢复。

### MP3D-tested 候选

以下数值来自论文/官方仓库，只描述其原任务，尚未验证能迁移到本项目的 t=0 evidence
contract：

1. **SFSS-MMSI** — Guttikonda 与 Rambach，WACV 2024，
   [论文实现与 checkpoint](https://github.com/sguttikon/SFSS-MMSI)。提供
   Matterport3D-trained 1024×512 spherical segmentation 模型；RGB-D checkpoint 的
   validation/test mIoU 为 39.191/35.921。它最适合先做 bounded pilot，因为同时使用
   RGB 与 depth、代码和权重已发布，且仍输出 image-space semantics。主要风险是固定
   MP3D label vocabulary 与本项目 channel 的映射、equirectangular 输入几何，以及旧
   PyTorch 1.13.1/mmseg 0.30.0 依赖。
2. **360BEV / 360Mapper** — Teng 等，WACV 2024，
   [官方实现与权重](https://github.com/InSAI-Lab/360BEV)。其 MP3D RGB panoramic
   segmentation checkpoint 为 46.35 mIoU；直接 RGB-D panorama→BEV 的 MP3D 模型中，
   MiT-B2/MSCA-B 为 44.32/46.31 mIoU。后者与最终 evidence 形状更接近，但同时替换
   segmentation 与几何投影，label space、坐标系、范围及 observed/unknown 语义都需
   重新验证，因此不作为第一轮最小可比 candidate。
3. **Geometric Exploitation for Indoor Panoramic Semantic Segmentation** — Dinh、Kim
   与 Cho，NeurIPS 2024，在 Matterport3D 等数据集上报告结果；但
   [官方仓库](https://github.com/caodinhduc/vertical_relative_distance) 当前只有
   README，并仍写明 code/data/checkpoints 将来更新，暂不可执行。
4. **Open Panoramic Segmentation (OOOPS)** — Zheng 等，ECCV 2024，包含 MP3D
   open-vocabulary panoramic evaluation；类别灵活性值得保留，但当前优先级低于已有
   MP3D RGB-D checkpoint 的 SFSS。
5. **SAI3D / OV3D** 等 MP3D 3D segmentation 方法依赖多视图、点云或更完整的 scene
   context。除非能证明只读取同一 t=0 observation，否则不进入当前 observation-bounded
   candidate set。

论文 mIoU 不能作为本项目 go/no-go。第一轮应在同一批 observation 上比较 YOLOE 与
SFSS 的映射后 category macro F1/IoU、投影后 semantic evidence 指标、coverage 与吞吐，
并以 oracle t=0 evidence 为固定参照；只有 bounded pilot 通过预先固定的门槛，才生成
完整 evidence 并训练 matched predictor。

### 观测表示与分支边界（尚未实现）

不应把现有 12-view 路径静默替换为 equirectangular，也不应维护两套下游 evidence
pipeline。首选边界是保留带 intrinsics/extrinsics 的 12 个 pinhole RGB-D views 作为
canonical raw observation，再由显式 adapter 生成候选模型所需的 equirectangular RGB-D
与 validity mask；segmenter 之后继续输出同一个 semantic evidence contract。派生 panorama
只在性能需要时缓存，并记录 source artifact/projection provenance。

现有 12 个视图只有水平 yaw 变化且单视图垂直 FOV 为 90°，因此只能覆盖球面的中间纬度
带，不能拉伸或静默填充成“完整 360×180 observation”。第一轮可在现有可见带上验证
projection 与 seam；若 SFSS 明显受 polar coverage 限制，再把 Habitat equirectangular
sensor、cubemap 或增加 pitch views 作为独立 acquisition ablation，并显式记录它扩大了
输入观测范围，避免与“同一输入只换 segmenter”的结果混淆。

已保留 `exp/llm-grid-yoloe-parallel` 作为 YOLOE 实验线，不改名；从其 HEAD
`26ce121e624db5480e945aa43843b47dbe92305f` 创建本地分支
`exp/llm-grid-mp3d-semantic-evidence`。该名称覆盖 SFSS、360BEV 及输入表示比较，又不把
尚未选定的模型写死。可复用的 panorama projection / typed observation contract 若被
接受，之后单独合入 `main`；候选适配器、配置与 launcher 留在实验分支。目前尚未实现
或启动任何 MP3D candidate。
