# 2026-07-29

## P5 RGB-D segmenter benchmark

### P5.2：50-start cohort 封存

P5.2 在 reviewed run commit
`80780e5a8a3736dc666b8dd861c00ce55f4685d8` 上执行一次固定命令，
输出到 ignored package
`data/rgbd_segmenter_benchmark/r2r-val-unseen-50-v1/`。package 恰有两个
regular、non-symlink file：`cohort.jsonl` 为 20,781 bytes、50 rows，
SHA-256 为
`89ae70f3e489fa702c66110f9bd9e7666ba0e16a9fbc3ac20aaa91a95adef0ce`；
`manifest.json` 的外部 SHA-256 为
`d71f04f102d80df3799e5fea76162147ad76c060c82ccbca88813d8b14a0b191`。
selection SHA-256（canonical selected row bytes 不含换行）为
`32a7adddf32291f059eb1045e63e077a693ca64d6fdf6e7418b54dd5d3644cb2`。

封存 population 为 1,839 examples、393 observations、11 scenes；lexical scene
population 为 `63/7/33/64/55/33/28/41/6/21/42`，Hamilton quota 和实际
selected count 均为 `8/1/4/8/7/4/4/5/1/3/5`。最终选中 50 observations、
255 unique example aliases、11 scenes。ordered observation-ID fingerprint 为
`f133de85d34f08c3e1e8bda2face50a83ac15c4502fa2b27b18e3a50f97e9626`，
ordered-alias fingerprint 为
`21e8f4d45e60fd9af525abc1dc15de4d12f4f42ca63e1777c839e5685ac84170`。

source raw split/index/manifest SHA-256 仍分别为
`6140b46759fe332ee96aa849d4bb64e1c1829b8f65acee127355040a6ee23484`、
`0acc9d18aea6d369db4b0a73f8ab3eebbe8fab51257b34614d1a7c407443acb9`、
`be2d25890c01234dd1bb8191af6c1d87121330991299287b635205f2ebf0327a`。
fresh-process public validator 通过；独立 audit 未 import cohort implementation，
只使用允许的 episode identity/start-pose fields 和 pinned index metadata，
逐 byte 重建出相同 package。artifact SHA perturbation 不改变 membership/order，
selection 全程没有读取 NPZ semantic arrays、RGB-D、target support 或 model output。

运行前 code/leakage/test review 最终均为 PASS；focused cohort tests 为 77 passed，
完整 `tests/analyze` 为 372 passed，Ruff、ty 和 diff check 均通过。P5.2 因此完成，
下一项为 P5.3 shared raw-frame collector 与全 50 start exact oracle replay；本步骤
不改变 candidate license/preprocessing gate，也未授权下载或运行 checkpoint。

### P5.3：共享 RGB-D/raw-frame package 封存

P5.3 已在 clean `main` commit
`923d44bc53ced1f4685e62fd8086a01056d513c5` 上完成唯一一次成功的
no-overwrite publication。固定命令为
`python -m prior.analyze.d2026_07_29.rgbd_segmenter_raw_frames`，本地物理
GPU 0 为 RTX 3090，
UUID `GPU-9224ffcb-5f88-2ece-ae3f-fca02cf43533`。环境记录为 Python
3.8.19、NumPy 1.24.3、CUDA runtime 11.8、driver 570.133.07、
Habitat/Habitat-Sim 0.1.7；installed-distributions fingerprint 为
`529bd6e8f6b930986999dad11a9b8b6dfa828120d46190af9112dfb8e32abd08`。

正式 publication 前有两次未发布失败，均在修复、review、重新 preflight 后从
50 rows 的第 0 行重启，未 resume：

1. attempt 1 在 render/staging 前发现安装包 canonical name 是 `habitat`
   而不是 `habitat-lab`；commit `1e7e66a` 修复环境捕获。
2. attempt 2 在 observation `aacc1b4d30a7c064f3ed` 发现 target-local
   start Z 与 pinned float32 metadata 相差 1 ULP。根因不是需要放宽 replay
   tolerance，而是 experiment-local AABB adapter 在减法前把 Magnum
   float32 center/size 提升为 float64。commit `d711bb0` 恢复 float32
   `center - size / 2`，再把结果提升为 float64 存储；core projector 和 exact
   replay gate 未改。11 scenes/50 observations 的 live metadata scan 从旧实现
   49/50 变为 50/50。43 个 targeted tests、351 个 collector/package tests、
   Ruff、ty、diff check 与独立 review 均通过。

成功 package 位于
`data/rgbd_segmenter_benchmark/r2r-val-unseen-50-raw-v1/`，恰有 52 个
regular、non-symlink files：50 个 NPZ、一个 50-row `index.jsonl` 和一个
`manifest.json`。50 个 artifact 共 166,439,458 bytes，tree SHA-256 为
`116e82d2077057523e11222cd2a207133b75a85f7600b87d1ee4dfa1eebb63ec`；
index 为 243,340 bytes，外部 SHA-256 为
`38a68369f72654271648589c0d19508831a3b484473fe382dadeee457db04d17`；
manifest 为 56,531 bytes，外部 SHA-256 为
`7aa278dd3b511bbf38aa647e3294a7fb9476b35bf7a2eb505a5bf3d2cf6c1c87`。
artifact+index payload 共 51 files、166,682,798 bytes，tree SHA-256 为
`6b9c48460493bf8f50aeed408173c95210426723fb0c845324e87cfd4dad1c46`。
fresh-process public validator 使用 producer commit 再次通过 50/50；成功后未
再次运行 publisher，staging 与 scene-snapshot roots 均不存在。独立 audit
没有 import/invoke publisher，重新验证了 50 个 container commitments、850 个
NPY member byte/logical-array hashes、全部 sealed pose、44 个 scene assets 和
11 个 bundle hashes，结果 CLEAN。

输入仍绑定 P5.2 cohort/manifest/selection SHA-256
`89ae70...f0ce`、`d71f04...b191`、`32a7ad...cb2`，以及 oracle
manifest/index、raw split SHA-256 `be2d25...327a`、`0acc9d...cb9`、
`6140b4...3484`。collector/package/projector/mapping-source/mapping-payload/
sensor-config/asset-role SHA-256 分别为 `380716...e0ac`、
`eb7fcb...3c50`、`daa4bf...be75`、`8e8fbc...ede3`、
`0aedb9...47d`、`b5f68f...ea85`、`a2cb16...b94`。first-row smoke 与
11/11 first-per-scene controls 均通过；
asset role 仍为 required `glb/house/semantic_ply`、auxiliary `navmesh`。

每个 artifact 保留固定 17-member schema、12 个 `0,30,...,330°` collocated
RGB/depth/semantic views、256×256、90° HFOV、metric float32 depth。sealed
float64 start position 必须 exact；raw quaternion norm 只允许 exact float32
epsilon `2**-23`，normalize 一次后 modality pose 必须 `array_equal`，agent 与
sealed quaternion 使用 sign-equivalent `1-abs(dot)<=1e-12`。ego/target 的
semantic、observed、free 六个 replay arrays 对全部 50 rows 均
`np.array_equal`；target-local start/direction 只在既有 float32 save/load
boundary 后 exact 比较，没有可调 tolerance。sealed cohort 和 core projector
均未修改；pinned oracle arrays 仅在每个 raw artifact 已 render、编码、严格
回读后加载，未参与 cohort 选择或 RGB-D 生成。

### P5.4：RGB-D segmenter benchmark harness 与 synthetic contract 封存

P5.4 已在 reviewed clean commit
`88b0ec54141ccd1828f4f7411528c8a7f8e5fc63` 上完成唯一一次成功的
no-overwrite publication。固定 synthetic adapter 只读取 sealed RGB/depth，使用
40-class deterministic CPU logits、aspect-preserving resize 与 odd padding；它没有
checkpoint、scene/observation identity、pose 或 oracle 输入。NYU40 静态 mapping
覆盖 23 个 deployable categories 中的 17 个；sealed cohort support 为
`5447/6120 = 0.890032679738562`。

正式成功前的尝试均在 rename 前失败，final/staging 均由 fail-closed cleanup
保持不存在，修复经 review 后都从第 0 行重新运行：

1. P5.3 fresh child 首先因 import-time warning/diagnostic stderr 被拒绝；
   commit `b7a6ce4` 只在 module import 期间隔离噪声，validator exception 与
   validator stderr 仍保持可见并导致失败。
2. publication provenance 随后发现 ignored 4.86 GB checkpoint；问题不是提高
   capture 上限，而是 Git authority 必须能在大型 ignored tree 上有界运行。
   commit `0aa81bb` 加入 tracked-only hashing、bounded `check-ignore` 与可信
   tracked `.gitignore` subtree pruning；commits `71d9d0e`、`cb8a548` 把本地
   tool/agent/environment roots 纳入 root ignore authority。clean worktree
   attestation 随后通过。
3. 第一次完整 synthetic run 在 fresh publication child 内失败。根因是 child
   虽通过 `/proc/self/fd/N` 使用 descriptor-bound Conda loader，但其嵌套 P5.3
   grandchild 不继承该 fd，回退到 system `libstdc++` 后缺少
   `GLIBCXX_3.4.29`。commit `88b0ec5` 在 project import 前验证 held directory
   descriptor，将 descendant loader path 绑定为
   `/proc/<live-child-pid>/fd/N`，并在 OpenCV 改写环境后恢复该精确值；真实
   two-generation regression、207 个完整 package tests、Ruff、ty 与独立
   security review 均通过。P5.3 sealed code 未修改。

成功 package 位于
`data/rgbd_segmenter_benchmark/synthetic-contract-v1/`，恰有 53 个 regular、
non-symlink files：50 个 prediction NPZ、一个 50-row `observations.jsonl`、一个
100-row `timings.jsonl` 和一个 `manifest.json`，共 157,590,346 bytes，package
tree SHA-256 为
`f031393ca0ac5c22f97ce8ae4c2078ae9c2e7b6150581d9617b8a2db96588276`。
50 个 predictions 共 157,311,300 bytes，tree SHA-256 为
`c55ea692b9fcfa01855625ddf8a280cf3dfc15e214e185724034e3892b39b2a6`；
`observations.jsonl` 为 221,145 bytes，SHA-256 为
`019995d3736568dd3585d5079d349bac30964e4251526aef4c0b0fb2a3af8f41`；
`timings.jsonl` 为 39,300 bytes，SHA-256 为
`76ad4789dd7a059ce6db9b6cce9eaf9177991a5ca6aa0eac6bfb304d35d3084b`；
`manifest.json` 为 18,601 bytes，外部 SHA-256 为
`53ceedaa82229051160fa93ed222e74e99ebf603f28b7f267de7fa20cd3d6986`。

fresh-process public validator 返回 `PASS`，candidate status 为
`NOT_APPLICABLE`，并确认 `synthetic_semantic_scores_exposed=false`；canonical
endpoint-free validation record 为 723 bytes，SHA-256 为
`e23a6cdc51bb2f33bd80991fb97d483380e6848d0e915a5f76910fdcd658c7a2`。
P5.3 与 benchmark attestation SHA-256 分别为
`8ad2463dd782ad3e7185850b63ee952b1c160e883dfee5259948d97d576ef6cc`、
`b499b498ce46f20e972aaf33f1547f9c939277913850d79ff53209bb28cf7578`。
独立 endpoint-free audit 未读取或报告 synthetic semantic endpoints，验证了
13 个 `0700` directories、53 个 `0600` single-link files、50 个跨 11 scenes
且 ordinal 恰为 `00–49` 的 prediction paths、所有 row/file/tree bindings 以及
producer/raw-producer commits，结果 CLEAN。contract、leakage、artifact、
timing、test、security 与 scientific recomputation reviews 均为 ACCEPT。
最终完整 `tests/analyze` 为 1076 passed、5 skipped，Ruff、ty 与 diff check
均通过。
成功 publication 后未再次运行 publisher，staging path 不存在。

本次 synthetic run 只证明 typed adapter、projection、metric、timing、artifact、
atomic publication 与 fresh validation contract 可执行；`decision` 为
`NOT_APPLICABLE`，`latency_claim=false`，timing backend/unit 为
`deterministic-fake`/`synthetic-tick` 且 `timing_comparable=false`，因此不形成
semantic quality、candidate GO、latency、resource 或 deployability claim。
P5.2 cohort、P5.3 sealed raw package/collector/validator 以及 core projector/code
均未修改，两个 sealed packages 均保留。

P5.4 至此完成。用户随后明确授权把官方 ESANet checkpoint 用于本次内部分析，
但该授权不等于上游 weight license，也不允许重分发。P5.5 已解除下载 blocker，
进入 artifact/environment/technical-smoke gates。

### P5.5：ESANet artifact gate

选定 official `TUI-NICR/ESANet` revision
`820c5bb633e49e69dcd075d4330165bb540a0cc9`，Google Drive object
`1w_Qa8AWUC6uHzQamwu-PAqA7P00hgl8w`。官方 archive
`nyuv2_r34_NBt1D_scenenet.tar.gz` 为 174,833,589 bytes；下载前确认本地约有
932 GiB 可用空间，archive 加解压峰值约 0.35–0.5 GiB，因此容量合理。

archive 的 host CRC32C `3b1c2ad1` 与本地结果一致，SHA-256 为
`ffe69568e107471d18a31493ef4aae2aa7aa1572ead51d072c4f02d8b4aef47d`。
解压前的只读 tar audit 确认 archive 只有一个 188,218,765-byte regular member
`nyuv2/r34_NBt1D_scenenet.pth`，没有 link、special file、重复/绝对/父级穿越
path；checkpoint SHA-256 为
`6b84f77dee42739fd3c5dd9e6b278450fa14e6977e2ec1609060eb6eb05cf456`。
PyTorch 2.1.2 在 CPU 上以 `weights_only=True` 成功读取唯一 top-level
`state_dict`：898 个 string-keyed tensors、47,006,371 tensor elements、
188,025,880 tensor bytes。后续禁止回退到 unrestricted pickle load。

上游 source 保持 detached、clean 且未修改；archive/checkpoint/source 均位于
ignored candidate storage，不进入 Git。source code 是 Apache-2.0，但 checkpoint
license 未说明；本轮只按用户明确批准的 internal-analysis risk exception 使用，
不声称 weight 获得 Apache-2.0 授权。详细 scope、hash 与 stop conditions 见
`docs/superpowers/specs/2026-07-29-esanet-weight-risk-exception.md`，实施 gate 见
`docs/superpowers/plans/2026-07-29-esanet-p55.md`。

shared `pyproject.toml`、`uv.lock` 和当前 environment 均未修改。另建 ignored
experiment-local overlay，只加入 Python 3.8 所需的 `pandas==2.0.3`、
`pytz==2025.2`、`tzdata==2025.3`；三只 wheel 的 SHA-256 均由 tracked
requirements hash 锁定。requirements SHA-256 为
`400cf8bbe845b1e677c772cc6d3238fbbfb1e39103e5dbfc50c2150017ffb6a3`，
base `pyproject.toml`/`uv.lock` SHA-256 分别为
`3b411d4c0c3322006a5d6500292a604c34276bd9c224746ed78b924afba5348e`、
`142cbd37a450be3e46cb585335972f87e2ef9cd14b81586e6b0d4609c9f5791e`。
overlay 共 2,813 files，RECORD、size、mode、file type 与 single-link audit
通过，tree SHA-256 为
`1673bdeeb522bd437eba149b83a2ad11d39f9e59fcacad43223cde05f2de3995`。
失败下载 partial、旧 wheel 和 source `__pycache__` 已清理；final archive 与
checkpoint 均规范化为 read-only non-executable data。

fresh CPU process 只做 semantic-blind technical smoke：显式构造 native
256×256、NYUv2-40 dual ResNet-34/NonBottleneck1D ESANet，禁用 ImageNet
初始化，以 `weights_only=True` 和 `strict=True` 加载，zero missing/unexpected
keys；模型参数量为 46,955,840。zero RGB/depth 输入的输出为 finite float32
`[1,40,256,256]`，两次 source-label bytes 完全一致，SHA-256 均为
`57ca797554874e721cd94c7a192eb2c8913e96c417e6291ac8f925aac40d2681`。
checkpoint ordered key/shape/dtype/bytes fingerprint 为
`52d436ab959e79c552617b04aab513a681ed5086d67add5773d260c6603776bb`；
本步未查看 label distribution、IoU 或任何 semantic endpoint。adapter 的
official-preprocessing differential 随后逐 tensor exact 通过。

再以 `CUDA_VISIBLE_DEVICES=4` 在空闲 RTX 3090
`GPU-f68964a1-66ca-1933-4b14-223315fdd647` 上读取 sealed ordinal 0 artifact
`observations/2azQ1b91cZZ/00-bd81990a0f88c2c285ac.npz`；输入 NPZ SHA-256 为
`1ca2f07e10eb29ae162db81b29fb662b79e68acd6bd31d9a0025bbb88d8c99cf`。
12-view native batch 输出为 finite float32 `[12,40,256,256]`，两次
source-label SHA-256 均为
`80c0f5f37d3318549c1d034c095e4a9b4506ea0b9e8e707fa526e6a0df476192`，
peak allocated 为 675,037,184 bytes。该 smoke 只验证 RGB/depth preprocessing、
native alignment、shape/dtype/finite、batch memory 与 determinism，不计算或
显示 label distribution/IoU/semantic endpoint。

experiment-only adapter、requirements 与 focused tests 位于
`prior/analyze/d2026_07_29/`、`tests/analyze/`；shared P5.4/core code 未修改。
sealed 12-view smoke 已纳入 tracked CLI `--sealed-batch` 与 focused GPU4
subprocess regression，不再依赖一次性命令；adapter boundary 也显式拒绝
logit shape/dtype/device/finite drift。complete overlay-active installed-
distribution fingerprint 为
`2a02828d8a98ae9336a9a94b7e885a357d09489d01eb489a91506f94ee6fdecd`，
source module origins 与 clean Git authority 在 inference 后重新验证。
focused tests 为 11 passed，Ruff、ty、diff check 均通过。P5.5 technical gate
因此完成，下一步为 P5.6 reviewed no-overwrite 50-start official CUDA run。

### P5.6：ESANet benchmark publication 准备

新增 experiment-only publisher 与 focused tests，不修改 P5.4 frozen
contract/package API、sealed inputs、upstream source 或 core code。运行固定使用
copy-based、无 pip 的独立 venv；只从离线 hash-locked wheels 安装
`pandas==2.0.3`、`pytz==2025.2`、`tzdata==2025.3`。site-packages 恰有
2,813 个 wheel-recorded files，完整 environment SHA-256 为
`a0342fd075394f31c9ee3d77f64278c6189b2137f88a9c47d12f09f8f6fd925c`。

publisher 固定 GPU 4 UUID、20 次 warmup、两轮各 50 个 measured observations
及 12-view float32 batch；CLI 不提供 experiment override。启动前要求 clean
committed worktree、final/staging path 不存在、精确 copied interpreter、
`PYTHONNOUSERSITE=1`、`PYTHONDONTWRITEBYTECODE=1`，并拒绝 `PYTHONPATH` 与
allocator override。inference 后再次验证 loaded upstream module origins、
source/checkpoint/permission/environment bytes；checkpoint 从已测量且
digest-bound 的 anonymous buffer 加载。只有完整成功 package 能进入 fixed
destination。frozen OOM factory 要求在执行前绑定 archival before/after CUDA
evidence，无法诚实记录实际 post-OOM snapshot；因此本 runner 遇到
contract-minted CUDA OOM 时也不发布 aborted package，只 fail closed，且不能
缩小 batch 重试或产生成功语义结论。

commit `97053ff` 后的第一次正式命令在 model inference/publication 前安全停止，
final/staging path 均未创建。fresh P5.3 child 使用 candidate venv 时，sealed
raw validator 报告 environment mismatch；根因是 candidate venv 按 ESANet
需要增加了 pandas/pytz/tzdata，而 raw package 必须重放原始、没有这三项的
installed-distribution set。直接使用原始 base interpreter
`/home/vscode/.conda/envs/etpr1-uv/bin/python3.8` 完整 replay 通过，返回
environment SHA-256
`35be66f579798cbdee8c4717da06ed7c4449fe0094580e8829b0acc330e483b7`。
frozen contract 将 `P53ValidatorLaunch` 与 candidate
`BenchmarkEnvironmentAttestation` 分开绑定，因此修复只在 experiment
publisher 中为 P5.3 固定原始 interpreter/environment，ESANet inference 仍固定
独立 venv；不修改或放宽 P5.3/P5.4 core validation。修复后的正式命令从
observation 0 重启并通过 P5.3，随后仍在 model inference/publication 前安全
停止，final/staging path 均未创建。第二个诊断发现 copy-based venv 没有 P5.4
fresh child 必需的私有 C++ runtime-loader alias；该 gate 正确拒绝回退到
system library。

从 base environment 复制 `libstdc++.so.6.0.34` 到 ignored venv `lib/`，并建立
relative `libstdc++.so.6` alias。target 为 21,295,144 bytes、mode `0555`、
single-link，SHA-256 为
`9581ad615b7c073423f57b69a3b148a89f8ea76fc909124211f9007909b807a6`；
P5.4 `_open_runtime_loader` 的真实 descriptor audit 已通过。新增文件不改变
installed-distribution fingerprint，candidate environment SHA-256 仍为
`a0342fd075394f31c9ee3d77f64278c6189b2137f88a9c47d12f09f8f6fd925c`。
publisher 另增加 exact alias/target/hash/mode/link preflight；不修改 P5.4 core。
修复后的正式 GPU publication 尚未运行。

### 分析任务检查清单

- [x] **P0：真实起点中心四向注册 oracle 诊断。**
- [x] **P1：跨语义族交叉拟合与 pivot controls。**
- [x] **P2：冻结 target-free 四路 hypothesis 的选择/聚合契约。**
- [x] **P3：实现并运行 cache-only target-free 离线实验。**
- [ ] **P4（blocked）：navigation ablation。** P3 未通过预注册 GO gate，
  不运行。
- [ ] **P5（当前目标）：RGB-D segmenter benchmark。**
  - [x] **P5.0：冻结可执行 benchmark contract。**
  - [x] **P5.1：静态审计 dense candidates。**
  - [x] **P5.2：封存 50-start cohort。**
  - [x] **P5.3：封存共享 RGB-D/raw-frame package 并完成 50/50 exact replay。**
  - [x] **P5.4：已用 synthetic adapter 冻结 typed
    adapter/projection/timing/metric/artifact contract。**
  - [x] **P5.5：接入 pinned official ESANet，不从头实现 segmenter。**
    - [x] 用户批准 internal-analysis weight risk exception；禁止重分发，且不把
      source license 误写成 weight license。
    - [x] 下载、hash、safe archive inspection 与 restricted checkpoint schema
      gate。
    - [x] experiment-local reproducible dependency、source/import 与 runtime
      fail-closed gate。
    - [x] CPU one-view 与 sealed GPU 12-view
      shape/dtype/finite/alignment/determinism technical smoke；未先看 semantic
      quality。
  - [ ] **P5.6（当前目标）：在 frozen P5.4 harness 上运行并封存 ESANet
    50-start benchmark。**
  - [ ] **P5.7：独立 audit、GO/NO-GO 与是否需要第二 candidate 的决策。**
  - [ ] **P5.8–P5.9：另行冻结 deployable region head；仅在 front-end GO 后
    规划 navigation integration。**
- [ ] **P6：按明确失败模式触发几何/语义敏感性实验。** 默认 deferred，禁止
  无界 sweep。

## LLM-Grid-Oracle-t0 导航上界与 evidence-only 消融

### 目的

当前 matched LLM-Grid-Oracle-t0 predictor 在 observation-bounded GT-semantic
evidence 下提高了 predictor-quality Raster IoU，但尚未验证该增益能否转化为
导航 SR/SPL。后续导航实验应同时回答两个问题：

1. 完美的 `t=0` 可见语义能给当前 Try5 导航带来多大收益；
2. 收益来自可靠的局部 observation evidence，还是来自 LLM 对未观测区域的补全。

这里的“上界”只表示 **`t=0` perception/evidence 上界诊断**，不是完整 cognitive
map 或导航系统上界；LLM 输出仍可能不准确。

### 对照组

| cognitive-map input | 含义 |
|---|---|
| ordinary LLM-Grid | 当前 instruction-only LLM-Grid 导航基线 |
| evidence-only reconstructed map | 直接使用 `t=0` observed/free/visible GT-semantic 重建图，不使用 LLM completion；衡量可靠局部 evidence 本身的贡献 |
| matched LLM-Grid-Oracle-t0 | 使用与当前 episode 场景/起点匹配的 observation-bounded GT-semantic evidence 生成 LLM-Grid；衡量 LLM 能否在局部 evidence 之上提供有用补全 |
| PriorGT | 完整 GT cognitive-map 导航参考上界 |

关键解释：

- matched Oracle-t0 `>` evidence-only：LLM completion 提供了额外导航信息；
- evidence-only `>` matched Oracle-t0：LLM completion 破坏或稀释了可靠局部信息；
- matched Oracle-t0 `>` ordinary LLM-Grid：改进 RGB-D perception/evidence 路径可能有实际收益；
- matched Oracle-t0 `≈` ordinary、但 PriorGT 明显更高：瓶颈更可能在 predictor
  或表示，而不是 GT cognitive map 本身无用；
- 各组均接近：需要进一步检查 map encoder/导航策略是否能利用地图质量差异。

### 训练顺序

第一阶段不从头重做 pretraining。先让 ordinary、evidence-only 和 matched
Oracle-t0 共用同一个现有 Try5 pretrained checkpoint，并分别重新进行匹配的
DAgger finetuning；固定初始化、训练步数、seed、checkpoint 选择、cache 缺失
处理和评估分母。这样可以用较低成本隔离 cognitive-map source 的增量价值，并
避免把 source 差异和 pretraining 差异混在一起。

仅替换 cache 直接评估现有 DAgger policy 可作为更便宜的兼容性检查，但属于
out-of-distribution cache swap，不能作为上界结论。

如果第一阶段出现明确增益，或结果接近但怀疑共享 pretraining 限制了新地图的
利用，再进行 source-matched pretraining，并从相同上游初始化、相同 schedule
分别完成 pretraining + DAgger。该阶段才用于估计各 source 在充分适配后的可达
上界。GRPO 暂后置，先用 DAgger 判断方向。

### 前置检查

- 确认 ordinary、evidence-only、matched Oracle-t0 在 navigation `train`、
  `val_seen`、`val_unseen` 上均有 navigation-ready cache；只有 `val_*`
  prediction 时不能完成匹配 DAgger。
- evidence-only map 必须满足 Try5 map contract；缺少的 direction/trajectory
  metadata 不能从 GT route 偷取，应使用各组共享且可部署的 metadata contract，
  或显式修改候选定义。
- 使用 SR/SPL 作为主要结论，保留 paired episode 结果、scene-cluster
  bootstrap 和完全一致的 missing-cache 零分处理。
- GT 选择的旋转角只用于 predictor diagnosis，不得进入本导航实验。

### matched Oracle-t0 navigation-full 第一阶段提交

本轮先执行 `matched LLM-Grid-Oracle-t0`，暂不执行 evidence-only 和 GRPO。
导航 architecture/source 仍为 `try5`/`llm_grid`，policy 仍为
`LLMGridTry5Policy`；`Oracle-t0` 只表示 predictor 的 evidence provenance，
没有增加新的 cognitive-map source 或 policy。

新增 `navigation-full` cache scope，只生成 R2R `train`、`val_seen` 和
`val_unseen` 的 navigation-ready raster，不生成 mixed pretraining cache；
该 scope 允许 observation-bounded evidence，任一生成后 grid
parse/navigation raster 失败会使作业非零退出。split manifest 新增 `scope`
字段，复用不同 scope 的旧 namespace 会显式失败。另增加
`MODEL.MAP_ENCODER.llm_train_reference_model_key`：本实验以 ordinary
LLM-Grid cache 为 reference，要求 Oracle cache 覆盖 ordinary DAgger 使用的
全部 10,707 个 train episode，否则 DAgger 在创建环境前失败。ordinary
population fingerprint 为
`a35b89afa80e43bb16f0aed038fe1943605695a47be07c806428dae21a3ebd5c`。

artifact 命名固定为：

- predictor：
  `outputs/llm_grid/r2r-rxr-oracle-t0-v1-s2-e2-seed42/checkpoints/final`
- navigation cache key：
  `llm-grid-oracle-t0-v1-e2-pseed42-matched-r1p5-d5-s2-navfull-v1`
- DAgger run ID：
  `exp_r2r_try5_llmgrid_oracle_t0_v1_matched_e2_pseed42_sharedpt460k_dagger_nseed100_r01`
- 预注册主 checkpoint：
  `<run>/store/ckpt.iter28000.pth`

ordinary `ckpt.iter28000.pth` 的内嵌 config 确认其 pretrained checkpoint 为
`model_step_460000.pt`、root navigation seed 为 100；因此本轮显式固定相同
配置。login node 上该 pretrained checkpoint SHA-256 为
`3ea8868fcb6d5d225b906cbe4ea6781fd7fc76697842ca5ed7a29761fce0fffb`。
Oracle predictor 先在本地压缩，传输 archive SHA-256 为
`8b5e644754b5a530579a424a772f4bf3e8ffb1da1df613a82c10bb375df3d61c`；
因长连接传输不稳定，改为 25 MiB chunks 后在远端重组，重组 SHA 完全一致，
解压后为 469 MiB，所有临时 archive/chunk 已清理。

代码分层：

- reusable commit `97687e0` 已进入 `main`；
- experiment branch `exp/llm-grid-oracle-t0-navigation` 当前提交为
  `cc69aaa`；
- login node 已切到该 branch；原有未提交的 ordinary GRPO checkpoint
  `iter500 → iter460` 修改已原样恢复并保留，没有混入本实验 commit。

验证结果为本地 focused 60 passed，login node focused 33 passed；Ruff、ty、
shell syntax 和 diff check 均通过。login node 提交前剩余约 130 GiB，因此
DAgger launcher 同时启动既有 checkpoint pruner，按 `loss/IL_loss` 保留 3
个 root checkpoint，并在 `iter28000` 成为完整 PyTorch ZIP 后复制到不参与
自动清理的 `store/`；训练正常结束后再执行一次 final prune。

2026-07-29 提交：

- navigation-full cache job：`1202940`，8 GPU，提交后在 `g0042` 运行；
- matched Oracle-t0 DAgger job：`1202941`，4 GPU，
  `afterok:1202940`，当前等待 dependency；
- evaluation 未提交，待训练结束后人工运行
  `scripts/submit/llm-grid-oracle-t0-matched-try5-dagger-eval.sh`。

本记录只表示实验已提交，尚不表示 cache 成功完成或导航指标已有结论。
