# 2026-09-24

## MIP 环境与数据核查（CLI 任务书 #1）

执行方式：用户出于安全考虑不在本地服务器部署 CLI，改由 Claude Code 云端容器执行、
以 GitHub 分支 `claude/trusting-brown-1hqo4c` 交付。完整报告见
`docs/reports/01_mip_task1/task1_report.md`，脚本与日志在同目录 `scripts/`、`logs/`。

### 已确认

- [x] MIP `1da7f1d0`（2026-09-18）+ 子模块 EmbodiedScore-envs `ea4c2ae6`（v0.0.1）克隆成功。
- [x] `python3 -m venv` + `pip install -r requirements.txt`：habitat_sim 0.3.3（GitHub Release 轮子，cp311）、
      embodiedscore-envs 0.0.1（editable）、litellm 1.83.4、mini-swe-agent 2.4.5、claude-agent-sdk 0.2.110。
- [x] `pytest tests`：10 passed / 1 skipped（skip 原因：无已录制 claude-sdk run 可回放）。
- [x] 模型表在 `exp_workspace/bareES/configs/models/models.yaml`；mini 座位支持 `api_base`（写入 litellm kwargs），
      cc / codex 不暴露 `api_base`；新增模型只需加一行 yaml。
- [x] rand100 是独立 episode 文件（`splits/r2r/rand100/rand100.json.gz` + `_gt`），非 id 列表；
      100 集、97 个 trajectory_id、无 `instruction_id`、`reference_path` 为 habitat 坐标列表。
- [x] FGR2R val_unseen 783 条；`new_instructions` 需 `ast.literal_eval`，`chunk_view` 为 1-based `[起, 止]` 视点区间；
      rand100 的 trajectory_id **100/100** 命中。
- [x] 坐标映射：`mp3d(x, y, z) = (hab_x, −hab_z, hab_y)`。R2R `path[i]` 与 CE `reference_path[i]` 同长同序，
      水平误差 < 0.05 m 的有 94/100（< 0.5 m 96/100，其余 4 集单点 navmesh 吸附偏差 ≤ 1.28 m）。
      竖直方向恒差约 1.38 m（connectivity pose 是相机高度，reference_path 是地面点），
      任务书原判据"3D < 0.5 m"因此 0/100，属判据口径问题而非数据不对应。

### 未能完成及原因

- A7 三项、C2：容器无 MP3D 场景（不可下载），且无 GPU / 无任何 EGL vendor
  （`WindowlessEglApplication::tryCreateContext(): cannot get default EGL display: EGL_BAD_PARAMETER`）。
  runner 在 hydra 配置、env server 起停、health、代码快照阶段均正常，卡在 `configure()`。
- B1 完整 val_unseen（1839）：下载源 Google Drive / `dl.fbaipublicfiles.com` 被容器网络策略拒绝。
- C1：无任何 provider key；OpenAI / DashScope / OpenRouter 域名被拦；仅 `api.anthropic.com` 可达，
  且容器内 `claude` CLI 已 OAuth 登录（订阅），即 `harness=cc` 的认证条件具备。

### 数据与仍未验证的假设

- 数据：`splits/r2r/rand100/*`（MIP 自带）、`Fine-Grained-R2R/data/FGR2R_val_unseen.json` 与
  `source/R2R-original/R2R_val_unseen.json`、本仓库 `precompute_img_features/connectivity/`（90 scan）。
- 假设 1（未验证）：rand100 上的一一对应结论可推广到全部 1839 集 val_unseen。
- 假设 2（未验证）：4 个有 ≤ 1.28 m 单点偏差的 episode 是 VLN-CE 生成时的 navmesh 吸附；未逐集查看。
- 假设 3（未验证）：mini 座位对 `openai/` 前缀自建端点仍需 `OPENAI_API_KEY` 有任意值（litellm 行为），本机无 key 未测。

### 下一步（待用户决定）

- 在有 MP3D 与 GPU 的机器（登录节点）上重做 A6–A7、C2；认证选 `claude` 订阅（cc）或 API key（mini）。
- 是否放开云环境网络策略以补做 B1 总数核对与 C1 探测。
- 确认 B4 的"水平 < 0.5 m"判据可作为 O-进度自动构造的对齐规则。

### 服务器执行方案（GitHub 为载体）

- [x] 用户决定：Claude 写脚本、用户在云服务器执行、结果经 git 推回；在服务器上新建独立目录
      （默认 `~/agentic-nav`），不动 `~/code/ETP-R1-snapshot/ETP-R1`。
- [x] 新增 `tools/01_mip_task1/run_task1.sh`（任务书 A0–C3 的幂等编排：按步骤写 PASS/FAIL/SKIP，
      失败重试至多 2 次，只在 WORKDIR 内写文件，A7-3 与 C2 需 `RUN_PAID=1 MODEL_A7=...` 才执行）、
      `collect.sh`（把报告、summary.json、日志拷回 `docs/reports/01_mip_task1/server/<host>_<date>/` 并提交）、
      `checks/*.py`（B1–B4、C1、C2 汇总，环境变量传参）。
- [x] 数据目录改为 WORKDIR 内实目录 + 逐 split 软链，避免向共享数据树写入 rand100 链接。
- 待验证：脚本在真实服务器（有 MP3D、GPU、conda 或 python3.10–3.13）上的 A5 自动搜索与 A7 是否通过。
- [x] 应用户要求把环境安装从 `run_task1.sh` 拆出：`check_env.sh`（只读体检：python/glibc/habitat_sim 版本、
      `pip --dry-run` 给出 requirements.txt 会带来的新增/升级/降级清单、EGL 空场景渲染探针、结论
      USABLE AS-IS / AFTER INSTALL / NOT USABLE）、`install_env.sh`（新建或向指定解释器安装，结束后自动体检）；
      `run_task1.sh` 的 A2 只验证 `PY=` 指向的解释器，不再安装。
- [x] 服务器（admin123-WZ-SERVER，4×RTX 4090，驱动 570，glibc 2.31）：ETP 环境 `etpr1-py38` 为 Python 3.8，
      判定 NOT USABLE；`install_env.sh` 用 conda 建 `MIP/envs/mip`（3.11）通过。EGL 探针最初失败，
      `egl_probe.py` 定位到根因：conda base 导出的 `__EGL_VENDOR_LIBRARY_DIRS=~/anaconda3/share/glvnd/egl_vendor.d`
      覆盖了 glvnd 的搜索路径，导致系统与轮子自带的 libEGL 都枚举不到任何 vendor；
      设 `__EGL_VENDOR_LIBRARY_FILENAMES=/usr/share/glvnd/egl_vendor.d/10_nvidia.json` 后渲染 OK，已写入 `egl.env`。
- [x] 服务器首轮 `run_task1.sh` 结果（`docs/reports/01_mip_task1/server/admin123-WZ-SERVER_20260924/`）：
      **A7-1、A7-2 PASS**（MP3D 在 `/data/xukai/mp3d`，90 scans；脚本化 agent 与 mini+fake 端点各跑通 episode 0），
      B1–B4 与云端结论一致；未找到 R2R-CE 完整 split；无 provider key，有 codex CLI 0.153.2，无 claude CLI；
      实际 8×RTX 4090。A7-3 / C2 待选模型。
- [x] 约定：CLI 任务的工具与报告目录一律编号（`tools/NN_<slug>/`、`docs/reports/NN_<slug>/`），本任务改为 `01_`，
      已写入 `AGENTS.md`。
- [x] 服务器补跑 A5/A6/B1/B4（`server/admin123-WZ-SERVER_20260924-0207/`）：R2R-CE 在
      `~/code/ETP-R1-main/data/datasets/R2R_VLNCE_v1-3_preprocessed_xlmr`（`_xlmr` 变体，episode 相同）；
      **完整 val_unseen = 1839 集**，613 条轨迹，无 `instruction_id`；B4 结论不变。
- [x] 任务书 #1 免费部分全部闭合，最终合并报告：`docs/reports/01_mip_task1/task1_report.md`
      （云端预跑记录改名 `cloud_container_report.md`）。A7-3 / C1 探测 / C2 因无模型 API 途径标 SKIP，等用户定 key 后作 02 号任务。

## 任务书 #2：O-进度 Oracle 与矛盾规则校准（零 API）

- [x] 新增 `tools/02_oracles/`：`run_task2.sh`（T2.0–T2.6 编排，逐步 PASS/FAIL/SKIP，报告每步重建）、
      `collect.sh`、`py/`（T2.1 k 匹配、T2.2 oracle、T2.3 `current_clause` 与 GT 回放、T2.4 预算、T2.5 规则误报、
      T2.6 已知失败回放、T2.0 summary 对比）。任务书原文存 `docs/reports/02_oracles/task_book.md`。
- [x] 本地用 rand100 冒充 train / val_unseen 做了端到端冒烟：全部步骤跑通，T2.6 正确 SKIP；数值仅验证流程，不作结论。
- 发现：按任务书严格规则（子句起点判定容差 0），rand100 GT 回放只有 64% 在终点落到最后一个子句——GT 停在距目标
  0.26–0.5 m 处，而最后一个子句多为起点恰在目标点的零长度 stop 子句。默认改用 `CLAUSE_TOL_M=0.5`
  （100% 终点命中，99% 单调），报告同时列出 {0, 0.25, 0.5, 1.0} 的对比，待用户确认。
- 待服务器执行：train（约 1 万集）与 val_unseen（1839 集）的真实数字；T2.0 需要一张 GPU 渲染一个假 episode。
- [x] 服务器首轮（`server/admin123-WZ-SERVER_20260924-0304/`）：T2.0 PASS（A7-(1) 复跑与任务 #1 一致），T2.1 起全部失败。
      根因：`CE_ORIG` 下的原版 R2R-CE 文件对 xukai 无读权限（`PermissionError`）。同时暴露 T2.0 的缺陷：
      目录不可读时 glob 不展开，脚本把 `_xlmr` 的 split 软链当"陈旧链接"删掉，只剩 rand100，却仍判 PASS。
      已修：运行前检查所有输入可读、否则什么都不改直接退出；T2.0 要求 train/val_seen/val_unseen 可读才 PASS；
      Python 崩溃记 ERROR 而非 FAIL。
- [x] 用户开通原版 R2R-CE 读权限后重跑（`server/admin123-WZ-SERVER_20260924-0423/`），T2.0–T2.5 PASS，T2.6 SKIP。
      最终报告 `docs/reports/02_oracles/task2_report.md`。要点：k 匹配 train 10819 / val_unseen 1839 全部精确命中；
      oracle 全部按序号对齐、区间连续（train 跳过 10 条第 4 指令）；0.5 m 容差下 GT 回放 100% 终点命中；
      v0 默认 R1=P99（2.45%）、R5=(0.5,2.0)（1.96%）、R6=(1.0 m, 10 步)（2.28%）；O-偏离在 val_unseen GT 上触发 3 条。
- 待决：容差 0.5 m 是否正式采用；R1 分位数预算只有 P99 达标，敏感度需真实失败数据检验；R5 过短误报；关键词分类粗糙。

## 研究 IDEA 重审（状态外置的 Agentic 导航）

- 对《研究提案：状态外置的 Agentic 导航》做了九视角独立评审 + 对抗核验，报告见
  `docs/reviews/2026-09-24_idea_review_state_externalized_agentic_nav.md`（41 条发现，F01–F30 经核验，0 条被否决）。
- 结论：IDEA 值得做，但命题、E0 设计、方法规格 v0 三处必须在任务 #3 执行前改。
  阻断级问题：E0 的 GT oracle 以文本追加进上下文，检验不了"载体"主张且 O-承诺缺席；
  矛盾规则 R1–R6 在 A/C 类主要失败上大概率不触发（satisfy 逐句放行并重置 R1，P99 预算过宽）；
  动机前提被同模型 max-effort 78 击穿；订阅 + harness=cc 路线与"模型只读状态"不兼容；
  C²Nav（2609.15142）、GPT-6-Astra 工作流（2609.20116）、Navi-Agent（2609.20388）、RegenHarness（2609.27612）等
  2026-08/09 新工作与贡献边界直接重叠（均仅见检索摘要）。
- 推荐定位改为"矛盾触发的强制决策 + 独立验证是 agentic 导航缺失的控制原语"的受控实验论文，三账本降为实现。
- 任务 #3 执行顺序建议改为 T3.0 → T3.4（增加 cc 会话/上下文控制必答项）→ T3.5 → T3.1 → T3.2 → T3.3；
  新增零费用项：规则在失败轨迹上的 ROC、回溯反放测试、RxR end_reason 统计。

### 评审后的决定与修订

- 用户决定：包装为**方法论文**；主模型 gpt-5.5（codex CLI 订阅，服务器已可用），第二模型 gpt-6-astra（本阶段不启用）；
  不再依赖 Claude 订阅。
- 提案 v1 写入 `docs/agentic_nav/03_research_proposal.md`（v0 的 00/01/02 参考文档一并归档到同目录）：
  主张改为机制"矛盾门控的承诺"（可撤销承诺 + 结构矛盾 + 强制 defend/revise，defend 经独立验证）；
  删除"只读状态/有界上下文"主张；E0 降为分析节；第一个付费实验改为 20 集方法先导 E-P；
  §九定点修订：commit 前置强制、satisfy 跨帧证据且不重置 R1、R1=P99、R5 只过长侧、R6 口径统一、验证器默认为主模型独立调用、解析冻结。
- 任务书 #3 v1 写入 `docs/reports/03_codex_baseline_oracle/task_book.md`：codex 路线；顺序 T3.0 → T3.4 → T3.5 → T3.1 → T3.2 → T3.3 → T3.6 → T3.7；
  20 集基线 + 3 集 oracle 冒烟并保存逐步轨迹；T3.6 实现分支偏离法；T3.7 零费用做规则 ROC、合成错分支召回、回溯反放。
  脚本 `tools/03_codex_baseline_oracle/` 待写。

### 任务 #3 首轮服务器结果（20260924-2023，合并报告 `docs/reports/03_codex_baseline_oracle/task3_report.md`）

- 通过：T3.0（final_only 容差 100%/100%/0，v0 参数 R1=P99 2.72%、R5 过长侧 0.22%、R6 2.66%）、T3.5（oracleES arm 注入成功，none≡bareES）、T3.6（θ=30°、d=1.0 m，误报 1.31%）、T3.7b（承诺 oracle 合成召回 84%）、T3.7c（开环反放误差中位 0.00 m，P90 ≤ 0.45 m）。
- 失败并已修：T3.1/T3.2 因 oracleES 复制了 bareES 配置导致实验名歧义；冒烟路径 bug。
- 基线：codex + gpt-5.5 20 集 SR 30，其中 5 集死于 OpenAI "at capacity"，有效 15 集 SR 40（MIP mini 座位 100 集 52）。新增 T3.3r 按索引重跑。
- **关键发现**：GT 上校准的 R1/R5/R6 在真实轨迹上成功集误触发 50–100%（R6 6/6），不能作硬门控；方法需改为"规则疑似 + 验证器确认"两级触发（待用户决定）。

### 任务 #3 第二、三轮（20260924-2128、20260925-0005）

- T3.1/T3.2/T3.3s 通过；T3.3r 补齐 5 集后 20 集干净基线：codex + gpt-5.5 SR 40 / SPL 23.2 / OSR 55 / nDTW 43.1（MIP mini 座位 100 集 52 / 44）。
- T3.7(a) 用补齐轨迹重算：R6 成功集误触发 8/8，R1、R5 各 5/8。用户接受两级触发（规则疑似 + 验证器确认），已写入提案 §9.4b，任务书 #4 v0 草案在 `docs/reports/04_ep_pilot/task_book.md`。
