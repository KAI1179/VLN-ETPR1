# 2026-07-28

## LLM-Grid 起点中心四向注册诊断

### 假设与固定协议

检验 LLM-Grid 绝对坐标上的低 Raster IoU 是否主要来自以已知起点为中心的
逐 episode 基数方向误差。仅回放本地 cache，保持 GT、起点和起始朝向不变；在 scale-2
`37x50x50` 栅格（`1 m/cell`）围绕连续起点 pivot，对 37 通道共享地搜索
`0/90/180/270°`。越出 `50x50` 框的 support 保留为 false positive，无效预测以空栅格留在分母。

不确定性使用 11 个 scene 为 cluster、seed `42`、10,000 次重采样的 paired
scene-cluster bootstrap。四个 GT-选择角度仅是 oracle 上界诊断，不是可部署的选择器，也未传入 navigation。

### 来源与验证

- 预测 cache：`data/llm_navigation/llm-grid-r2r-rxr-r1p5-direction5-s2-tagfree-epoch-2/r2r/val_unseen/`。
  cache key 为 `llm-grid-r2r-rxr-r1p5-direction5-s2-tagfree-epoch-2`，manifest SHA-256 为
  `31e7c5f8d186e75222b12f1aa8862b16fa9904c75221a0fd55cfba61f93fa8ce`。
- target：R2R `val_unseen`，GT cognitive-map namespace `gt.legacy.r1p5.direction5.v1`；运行 commit
  为 `85dd5610fb7d1f48957868e7d278a1fe0a17e949`。该 commit 已按
  `world_delta_to_direction_vector(dx, dz) = (-dz, -dx)` 的反射存储基修正 direction
  rotation：栅格的物理 `+theta` 对应存储向量分量的数值 `-theta`。
- smoke：`python -m prior.analyze.d2026_07_27.llm_grid_transform_diagnosis --limit 8 --output-dir outputs/llm_grid_analysis/start_centered_registration_smoke`。
  既有 smoke 协议覆盖 8 个不重复 episode、4 个角度和全部有限数值；本轮 artifact
  contract 回归固定为 7 个必需产物。
- 全量：`python -m prior.analyze.d2026_07_27.llm_grid_transform_diagnosis`。共 1,839 个不重复 episode、11 个 scene，
  1,830 个 schema-valid、9 个 invalid；9 个 invalid 仍在 CSV 中且 `delta_iou=0`。
- 7 个产物为 `manifest.json`、`episodes.csv`、`summary.json`、`bootstrap.json`、
  `angle_distribution.csv`、`iou_delta_distribution.png` 和
  `angle_distribution.png`；新增 CSV 的稳定表头为
  `angle_degrees,episode_count`，并按 `0/90/180/270°` 声明顺序记录
  `751/476/248/364`。
- identity 列为 `0.13570059094519527`，对比
  `outputs/llm_grid_eval/checkpoint-sweep-r1p5/runs/001/metrics.json` 的
  `0.13570059094519524`，绝对差 `2.78e-17`（小于 `1e-9`）。
- 逐行验证所有数值为有限值，out-of-frame support 非负，
  `in_frame + out_of_frame = warped`，`best_iou >= identity_iou`，角度属于声明顺序；
  angle count 和为 1,839，bootstrap CI 顺序正确。

### 结果

| 指标 | 值 |
| --- | ---: |
| identity / 四向 oracle episode-macro IoU | 0.135701 / 0.186357 |
| 配对增益均值 / 中位数 | 0.050656 / 0.026497 |
| 95% scene-cluster CI | [0.046979, 0.054718] |
| `delta > 0.01` / `delta > 0.05` | 56.12% / 41.05% |
| 角度 0 / 90 / 180 / 270° | 751 / 476 / 248 / 364 |
| identity 比例 | 40.84% |
| best-second margin（2.5% / 50% / 97.5%） | 0.000000 / 0.060598 / 0.203298 |

敏感性：object 从 `0.153862` 到 `0.211765`（`+0.057903`），region 从 `0.100563` 到
`0.138174`（`+0.037610`），mentioned/unmentioned 从 `0.120358/0.135254` 到
`0.168913/0.184877`；occupancy-collapsed 从 `0.297598` 到 `0.411155`（`+0.113558`），
排除 broad object channel 后仍从 `0.076032` 到 `0.104261`（`+0.028230`）。预测 direction
vector cosine 在修正反射存储基的旋转符号后从 `0.212705` 变为 `0.163500`。修正值高于旧的
错误符号结果 `0.106951`，但仍低于未旋转值；因此当前 oracle raster 角度仍不是同时修复
raster 和 direction metadata 的证据，旧的 `0.106951` 不再作为实验结论。

support 无负值：input/warped 总数 `411751`，in-frame `402881`、out-of-frame `8870`；
平均每 episode out-of-frame 为 `4.8233`（中位数 0）。检查两张 PNG 后，坐标轴和标签未被裁剪，
图面非空白，无需要图例且不存在不可读图例或误导性轴。

### 决策与下一步

**Go（仅对四假设计实验）。** 停止门槛需要同时满足宏观增益小于 `0.01`、CI 包含 0、且只有少数
episode 超过 `0.05`。本次增益 `0.050656`、CI 完全为正、且 41.05% 的 episode 超过 `0.05`，故通过。

这不使 oracle 角度可部署：它由 GT raster 选出，不能作为 LLM-Grid 或 navigation 输入。下一步是在
不使用 GT 选择的前提下，设计并测试四路旋转 cognitive-map hypothesis 的可部署选择/聚合契约；只有
契约明确后才进入对应的 navigation 实验。

## LLM-Grid 起点中心跨语义族交叉拟合对照

### 冻结问题与协议

本轮只问两个预注册问题：由一个不相交语义族选择的角度，能否提高另一语义族的
held-out IoU；这种迁移是否在 episode 的真实起点 pivot 上强于几何上合理的对照 pivot。
沿用本地 cache key
`llm-grid-r2r-rxr-r1p5-direction5-s2-tagfree-epoch-2`、R2R
`val_unseen` 和 GT cognitive-map namespace
`gt.legacy.r1p5.direction5.v1`。运行 commit 为
`9ee2dfcbaf6da452620bf81d99588f9d04e413c1`；固定总体为 1,839 个不重复
episode、11 个 scene、1,830 个 schema-valid 和 9 个 invalid-or-missing，后者以空预测保留在完整分母。

语义族固定为 object 通道 `[0, 27)` 与 region 通道 `[27, 37)`，分别执行
`object→region` 和 `region→object` 交叉拟合。三个连续 pivot 是真实 scale-2
起点、map center `(25.0, 25.0)`，以及 seed `43` 的 scene 内一对一 shuffled
start；角度顺序固定为 `0/90/180/270°`，平局按声明顺序保留 identity。推断使用同一个
paired scene-cluster bootstrap multiplicity matrix，以 11 个 scene 为 cluster、seed
`42`、10,000 次重采样、episode-macro 权重和 2.5/97.5 百分位区间。

### 验证与结果

独立 artifact validator 验证了 7 个固定产物，并对 6 个非 manifest 产物执行 SHA-256
校验；同时逐项确认 1,839 个 identity、11 个 scene、22,068 个 angle row、11,034 个
cross-fit row、1,839 个 pivot assignment，以及角度/pivot/direction membership、
support 分解、配对算术、有限值、bootstrap 输入和 literal gate decision。

| endpoint | mean held-out `delta_iou` | 95% scene-cluster CI |
| --- | ---: | ---: |
| true start `object→region` | 0.033631 | [0.028280, 0.039238] |
| true start `region→object` | 0.041313 | [0.036857, 0.045414] |
| true start symmetric | 0.037472 | [0.032780, 0.042123] |
| map center symmetric | 0.000532 | [0.000000, 0.001077] |
| shuffled start symmetric | 0.005129 | [0.002798, 0.007162] |
| true start − map center | 0.036940 | [0.032119, 0.041642] |
| true start − shuffled start | 0.032343 | [0.028061, 0.037573] |

true-start symmetric 的 leave-one-scene-out episode-macro range 为
`[0.035973, 0.039000]`。各 pivot 和两个方向的 selector/held-out
`predicted_support_empty` 率均为 `9/1839 = 0.4894%`，对应保留在分母中的 9 个空预测；
`target_support_empty` 与 `union_empty` 率均为 0。按 `0/90/180/270°` 顺序，选中角度计数为：

| pivot | `object→region` | `region→object` |
| --- | --- | --- |
| true start | 756 / 478 / 239 / 366 | 895 / 418 / 207 / 319 |
| map center | 1817 / 15 / 3 / 4 | 1819 / 12 / 3 / 5 |
| shuffled start | 1559 / 133 / 38 / 109 | 1581 / 125 / 38 / 95 |

其中 identity 数量包含 identity-first 平局和 9 个 invalid 空 selector；因此 map-center
与 shuffled-start 的大量 `0°` 不能解释为可部署角度预测。

### 决策、限制与下一步

五项冻结门槛全部通过：

1. true-start symmetric 均值 `0.037472 >= 0.01`；
2. 其 CI 下界 `0.032780 > 0`；
3. 两个 directional 均值 `0.033631` 和 `0.041313` 均大于 0；
4. true start − map center 的 CI 下界 `0.032119 > 0`；
5. true start − shuffled start 的 CI 下界 `0.028061 > 0`。

故 literal decision 为 **GO**：这支持不相交语义通道间共享的、真实起点中心的基数角
alignment 证据，不是此前同一 target 上选择并评分的 all-channel oracle 证据。
每个方向仍由 GT target raster cells 在 selector 语义族上选择角度，所以该结果不是
target-free，也不是可部署性能；任何 outcome 都不授权 navigation training 或使用
GT 选择角度。该 GO 唯一授权的下一步是设计 target-free 的四路旋转 hypothesis
selector 或 aggregator。

## LLM-Grid target-free 四向选择器离线检验

### 冻结运行与来源审计

固定命令
`python -m prior.analyze.d2026_07_28.llm_grid_target_free_cardinal`
在独立 code、leakage 和 test audit 全部通过后只运行一次；运行 commit 为
`f999a446868d14f7c011c1f92e9bf40e8ef6b7a6`。开发集严格为 R2R
`val_seen` 的 778 episode、53 scene、770 valid、8 invalid；检验集严格为
R2R `val_unseen` 的 1,839 episode、11 scene、1,830 valid、9 invalid。

schema v3 对实际读取的 dataset、prediction 和 cognitive-map member 做了
分阶段 byte fingerprint，并由独立进程重新生成全部九个 artifact：

| source phase | entries | SHA-256 |
| --- | ---: | --- |
| development all | 3,893 | `7b0f8ce883a4929679ea51627726e97fc318f9785cb2794afb5908f002a860cc` |
| test assignment | 5,520 | `e6065ad984f05bd42950fa52855310bc7364421d424d4e29704e508bda447d10` |
| test target | 3,678 | `62307358663497a3bfb98220b4d2a875fe50fb0922ba020186cc6be6cbcf89ef` |
| combined | — | `5e8702088364ee294ad0876486d403fe1b68283897bff2468369167c262bf942` |

`val_unseen` assignment CSV 在 target member 被读取前完成序列化和 SHA-256；
assignment hash 为
`24e9dc0b158116b82de341517597736f2600c5bfdb1a205ad6fb5dcd1c368e20`。
development/test source、combined source、population contract、assignment-before-target
和 post-score source recheck 的全部 audit boolean 均为 `true`，总 audit 为
`true`。九个 artifact 的外部锚点 `manifest.json` SHA-256 为
`e700a4765bf867c0e9d6952ecab4ea9b4266e491ed809fd25bdaee429eaa9e2f`。

### 锁定选择与结果

`val_seen` 在八个 heading mapping 中唯一锁定
`angle = (quantized_start_heading + 90°) mod 360`（`sign=+1`,
`offset=90°`），development IoU 为 `0.12095691473790687`。四个 global
候选唯一锁定 `0°`，即 identity，development IoU 为
`0.180875151023238`。

| candidate | all IoU | object IoU | region IoU |
| --- | ---: | ---: | ---: |
| identity / global 0° | 0.135701 | 0.153862 | 0.100563 |
| primary heading selector | 0.111363 | 0.127626 | 0.079112 |
| direct direction selector | 0.117940 | 0.133326 | 0.087712 |
| random expectation | 0.114568 | 0.130564 | 0.082997 |
| soft identity | 0.135701 | 0.153862 | 0.100563 |
| uniform soft aggregate | 0.112253 | 0.127606 | 0.080990 |
| four-way oracle ceiling | 0.186357 | 0.211765 | 0.138174 |

primary 相对 identity/global 的 mean delta 均为 `-0.024338`，95%
scene-cluster CI 为 `[-0.030278, -0.018036]`，LOSO range 为
`[-0.025869, -0.022124]`。global 相对 identity 的 mean、CI 和 LOSO
均严格为 0。direct 相对 identity 为 `-0.017760`，CI
`[-0.022526, -0.012142]`；uniform soft aggregate 相对 soft identity 为
`-0.023448`，CI `[-0.028291, -0.017544]`。

primary 的 object/region delta 分别为 `-0.026236/-0.021452`；global
均为 0。soft identity 的 exact object/region IoU 为
`0.1538616241545797/0.1005634347772255`，soft aggregate 为
`0.12760578068449654/0.08099049865598874`；对应 exact delta 分别为
`-0.026255843470083168/-0.019572936121236756`，来自 soft raster，
不是 boolean angle score proxy。primary support 为 predicted/target
`411751/345669`，其中 in-frame/out-of-frame 为 `377898/33853`，
union 为 `691579`；soft prediction/target/intersection/union mass 为
`411751/345669/67398/690022`。invalid、empty prediction、empty target、
empty union 数量为 `9/9/0/0`。

按 `0/90/180/270°`，primary 角度计数为 `367/523/526/423`，global 为
`1839/0/0/0`，direct 为 `961/353/237/288`，oracle 为
`751/476/248/364`。primary/global/direct/soft 的 oracle-gain recovery
分别为 `-0.480457/0/-0.350604/-0.462886`。interval plot 已解码并人工检查：
坐标轴、标签和区间均清晰，两个 primary contrast 完全位于 0 左侧，
global−identity 位于 0。

### Gate、限制与下一步

selector 六项条件按冻结顺序为
`false/false/false/false/false/true`；global 五项条件为
`false/false/false/false/true`。两者均没有达到 `+0.01`，CI、语义族和
LOSO 条件也未通过；PARTIAL 条件同样为 false。因此 literal decision 为
**NO GO**。

这说明当前 start-heading mapping、prediction-direction direct selector 和
uniform soft aggregation 都不能把四向 oracle 上界转化为 target-free 改进；
其中 primary 和两个 descriptive baseline 反而稳定降低 IoU。该结果是在
已经被 P0/P1 使用过的 `val_unseen` population 上进行的 target-hidden
preregistered replay，不是 pristine held-out generalisation，更不是
navigation performance。

本结果不授权 P4 navigation ablation、training 或 core-code 修改，也不授权
看完结果后更换 mapping、阈值或 selector。若未来重启 cardinal selector
路线，只能另行预注册能区分具体失败机制的 follow-up；当前没有正向
descriptive evidence 支持立即重试。检查清单因此转向独立的 P5 RGB-D
segmenter benchmark，P6 仍只在有明确失败假设时触发。

## RGB-D segmenter benchmark 启动

P5.0 对 07/27 的方向性设计做了运行前冻结。固定 population 为本地 R2R
`val_unseen` oracle-evidence index 的 393 个 unique start、1,839 个 example、
11 个 scene；index/manifest/raw split SHA-256 分别为
`0acc9d18aea6d369db4b0a73f8ab3eebbe8fab51257b34614d1a7c407443acb9`、
`be2d25890c01234dd1bb8191af6c1d87121330991299287b635205f2ebf0327a`、
`6140b46759fe332ee96aa849d4bb64e1c1829b8f65acee127355040a6ee23484`。
50-start cohort
不再沿用任意的全局前 50 个 hash，而按 scene population 做 Hamilton
proportional allocation；11 个 lexical scene 的 quota 为
`8/1/4/8/7/4/4/5/1/3/5`。每个 scene 内只按 canonical observation row 的
domain-separated SHA-256 排序，selection 不读取 semantic array、RGB-D 或模型输出。

primary endpoint 已明确为排除 `void/structure/other/free-space` 后的 23 个
deployable object channel：每个 observation 将 `23×50×50` category-cell
flatten 后计算一个 IoU/F1，再做 observation macro；all-27、per-category 和
oracle-region composition 都只作 descriptive/non-deployable diagnostic。
50 个 observation 必须全部用现有 projector exact replay，不能只验证一个 smoke。

RTX 3090 上固定一次处理 12 view，20 个固定 warmup，随后按 manifest 顺序跑两遍
50 start；dense front end 的 hard gate 为 end-to-end P95 `≤1000 ms`、absolute
peak reserved VRAM `≤16 GiB`、mean observation IoU `≥0.15`、F1 `≥0.25`。
paired scene-composition robustness 使用 PCG64 seed `20260728` 的 10,000 次
scene-cluster replay；它不是对 393-start population 的 sampling CI。mapping、
projection、failure、Pareto、YOLOE fallback、atomic publication 和 fresh-process
validator 规则均已在看 candidate score 前冻结。

P5.1 primary-source audit 将 ESANet-R34-NBt1D 的 SceneNet-pretrained NYUv2
checkpoint 作为 conditional primary，将 DFormerv2-S 作为 blocked challenger。
两者共享的 NYU40 mapping 覆盖 23 个 deployable target 中的 17 个，明确不支持
`plant/shower/stool/fireplace/gym_equipment/seating`；80% target-cell support
gate 要等 cohort 封存后只计算一次。ESANet code 为 Apache-2.0，但外部 checkpoint
没有明确 weight license；DFormerv2 同时存在 MIT file、README non-commercial
限制和无 weight license 的冲突，且 depth PNG encoding 仍需澄清。因此当前不下载
或运行任何 candidate；先执行不依赖模型的 P5.2 cohort sealer。

## 后续实验优先级检查清单

以下列表依据已经保留的设计、结果和 gate 重建，并作为删除旧 handoff 后的权威清单。
优先级表示依赖顺序；未通过上一项 gate 时，不得自动进入后续训练或 navigation。

- [x] **P0：真实起点中心四向注册 oracle 诊断。** 已确认 `0/90/180/270°`
  oracle 可将 episode-macro IoU 从 `0.135701` 提高到 `0.186357`，但不具备可部署性。
- [x] **P1：跨语义族交叉拟合与 pivot controls。** object→region、
  region→object、map-center 和 shuffled-start 的冻结实验五项 gate 全部通过；证据支持
  真实起点中心的共享基数角 alignment，但 selector 仍使用 GT。
- [x] **P2：冻结 target-free 四路 hypothesis 的选择/聚合契约。**
  明确运行时可用输入、四路地图的表示、selector 或 aggregator、tie/失败语义、
  identity 与无选择聚合基线、离线指标、统计方法和 GO/NO-GO gate；设计中禁止读取
  GT target cells、oracle angle 或其派生量。已冻结为
  `2026-07-28-llm-grid-target-free-cardinal-selector-design.md`：以 `val_seen` 锁定
  heading-conditioned mapping 和 global correction，先封存 `val_unseen` assignments
  再读取 target；selector 必须额外显著优于 global correction 才能判为 SELECTOR GO。
- [x] **P3：实现并运行 cache-only target-free 离线实验。** schema v3
  source/audit contract、一次固定运行和两项独立 artifact audit 均通过；literal
  decision 为 **NO GO**。primary、direct 和 soft aggregate 均显著差于 identity，
  global correction 锁定为 `0°`。
- [ ] **P4（blocked）：仅在 P3 通过预注册 gate 后运行 navigation ablation。**
  本轮未通过，故不运行 training 或 navigation，不得以事后 selector 或 threshold
  修改补救。
- [ ] **P5（当前目标）：推进 RGB-D segmenter benchmark。** 沿用
  `2026-07-27-rgbd-segmenter-benchmark-design.md` 的候选审计、共享 raw-frame contract、
  50-start 配对 benchmark 和 predictor/navigation bridge；不要把 GT region head 或
  oracle angle 包装成 deployable system。
  - [x] **P5.0：冻结可执行 benchmark contract。** 已固定 cohort、23-channel
    endpoint、region control、latency/resource、statistics、gate、provenance 和
    experiment-only-on-main 边界。
  - [x] **P5.1：静态审计 dense candidates。** ESANet 为 conditional primary，
    DFormerv2-S 为 blocked challenger；两者在 weight permission 澄清前均不下载或运行。
  - [ ] **P5.2（当前）：实现并封存 50-start cohort sealer 与独立 validator。**
  - [ ] **P5.3：采集共享 RGB-D/oracle raw frames，并对全部 50 start 做 exact replay。**
  - [ ] **P5.4：用 synthetic adapter 冻结 typed adapter/projection/timing/metric/artifact
    contract。**
  - [ ] **P5.5–P5.7：仅在 license 与 preprocessing gate 通过后运行 ESANet、必要的
    DFormerv2-S 及 conditional YOLOE fallback。**
  - [ ] **P5.8–P5.9：另行冻结 deployable region head；只有 front-end GO 才规划
    predictor/navigation bridge。**
- [ ] **P6：按失败模式触发几何与语义敏感性实验。** `5°` 细角度、translation、
  scale、reflection、其他 channel folds、mentioned/unmentioned 和替代 shuffle seed
  均默认 deferred；只有它们能区分 P2/P3 的具体失败假设时才预注册并执行，禁止无界 sweep。
