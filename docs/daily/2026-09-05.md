# 2026-09-05

## Refiner S3b 与 S4 速度门

### S3b

- 使用 S3 相同数据、seed 与超参数，仅增加同步 dihedral 增强。
- best epoch 为 9，`val_unseen / route_seen / object mIoU@10×10`
  为 32.7320%，高于无增强 S3 的 29.3840%。
- 按 task book 选用
  `data/refiner/checkpoints_aug/best.pt`，SHA-256 为
  `2a75a034b8db69b2673828285a43013bede5bd98eb04143a91f6f8e8d43a8f0d`。
- 完整表格见
  [`reports/refiner/S3b_refiner_augmentation_report.md`](../../reports/refiner/S3b_refiner_augmentation_report.md)。

### S4

- Control 与 Refiner 均从 Try5 `model_step_460000.pt` 开始，seed 100，
  4 ranks × 4 envs，运行 200 iterations；唯一模型差异是
  `MODEL.MAP_ENCODER.refiner_ckpt`。
- Control 为 12.220 秒/iter，Refiner 为 19.278 秒/iter，慢
  57.76%，超过 30% 硬门槛。
- Refiner 分项累计耗时：证据投影 396.499 秒，Refiner 输入/前向/
  合成 30.149 秒，其余 3428.976 秒。直接可归因的主要额外开销是证据投影，
  不是 Refiner 网络前向。
- 日志确认 Refiner 加载 7,827,749 个参数；episode 3267 的
  step 0 活跃空间格从 P0 372 变为 refined 349，说明分支已生效。
- 按 task book 在速度门暂停，未启动 30k DAgger 与 val_seen/
  val_unseen 评测。详细见
  [`reports/refiner/S4_dagger_speed_gate_report.md`](../../reports/refiner/S4_dagger_speed_gate_report.md)。

### 仍未验证

- 速度优化后的 Refiner 是否能通过同一 200-iter 速度门。
- Refiner 对 Try5 的 val_seen/val_unseen SR、SPL、NE 与 OSR 影响。
