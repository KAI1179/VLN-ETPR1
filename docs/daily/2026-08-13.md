# 2026-08-13

## 双地图交互方案第二次收敛

### 目标

根据讨论修订
[`upload_files/05_基于现有编码器的双地图交互方案.md`](../../upload_files/05_基于现有编码器的双地图交互方案.md)，进一步去除不可靠的 graph-grid 绝对坐标假设，并明确视觉语义与偏航监督。

### 结论

- 新架构只借鉴 Try5 的 map-to-graph attention 形式，不继承 Try5 fusion 权重；Try5
  保留为静态单向融合消融。
- graph-to-map 使用无绝对坐标的全局 attention：全部 visited 节点提供历史上下文，
  新观测触发整张 map-token 状态更新。
- 未来地图允许随已访问证据改变；保留不可变 `Z0`，递归学习全图残差，并用无新
  证据恒等、写门预算及 token 多样性约束防止状态坍塌。
- 删除“未见想象/视觉支持/认知冲突/视觉新增”四分类 head。无坐标对应时无法可靠
  构造逐格来源标签，该任务也不是导航闭环必需项。
- `VisualEvidenceHead` 保留为当前 panorama 的 37 类语义辅助任务。训练标签需由
  Habitat/MP3D Semantic + Depth 离线生成；当前 `data/` 下未发现可直接使用的逐
  viewpoint cache。验证与推理仍只输入 RGB-D。
- 偏航时 phase/remaining target 设为 `None` 并 mask；另监督 `off-route` 状态和
  每个 ghost 的 recovery 标签。recovery 表示候选能使 agent 明显接近 GT reference
  path，不能直接等同于当前 SPL teacher action。

### 尚未验证

- 全局更新能否在不使用 graph-grid 硬对齐时学习稳定的未来地图修订；
- 逐-viewpoint 37 类语义监督的生成成本、类别覆盖率和标签噪声；
- `3 m` off-route 阈值与 `0.25 m` recovery 改善阈值是否适合 R2R DAgger。

## 双地图在线交互实现

### 实现

- 新增 `online_fusion` 架构：visited 图节点无坐标地全局更新 100 个空间地图
  token；更新状态跨 DAgger 时间步保存；所有 ghost 从更新地图读取长时程信息。
- 新增 37 类视觉证据、`(37,100,100)` 稠密地图、五段进程、
  on/off-route/finished、remaining、recovery 与 ghost 排序监督；未引入 JSON 或
  在线 LLM。
- 预训练对 expert prefix 按访问顺序展开同一递归更新；DAgger 为并行环境同步维护
  地图状态、已消费 viewpoint 和路径进度，并在环境 pause 时同步裁剪。
- 参数组按方案固定前端 encoder；新交互模块为 `1× LR`，既有后端为
  `0.1× LR`。DAgger 强制完整加载本方案预训练权重。
- 新增本地 GPU 6、7 的预训练和 DAgger 启动脚本，以及训练期 panorama 语义
  标签生成器。

### 已验证

- 基础权重
  `pretrained/r2r_rxr_ce/mlm.sap_habitat_depth/store2/model_step_367500.pt`
  存在，且除新增 Map Encoder/online-fusion 子树外与预训练模型键严格匹配。
- R2R/RxR 混合预训练样本可同时读取 LLM-Grid 与认知地图 GT；一次真实样本的
  前向、联合损失和反向传播通过。
- 小模型预训练权重到 DAgger 模型的键映射完整，无缺失或额外键；共享交互模块、
  损失和语义类别映射单元测试通过。
- Python 编译和三个本地 shell 启动入口的语法检查通过。

### 当前外部前置项

- `data/online_fusion/panorama_semantics_v1.hdf5` 已生成并核对：10,567 个
  viewpoint，每项为 `(37,) float32`。
- 已用单步真实预训练产生完整 OnlineFusion checkpoint，并完成
  DAgger 真实 rollout 闭环验证。

## OnlineFusion 监督核对

- 新增 [`06_视觉语义缓存与损失监督核对.md`](../../upload_files/06_视觉语义缓存与损失监督核对.md)，按代码核对缓存生成、6 组辅助损失及进度标签。
- 全量缓存覆盖 90 个 scan 的 10,567 个有效 viewpoint；它只为离线预训练提供 37 类 panorama 语义 GT。DAgger 不读取该缓存、不创建 Semantic sensors，而是冻结 `VisualEvidenceHead`。
- `state` 是无 GT 正则；grid/visual 有训练 GT；progress、recovery 与 ghost 使用 GT/expert 派生标签。预训练没有 off-route/recovery 样本，相关监督只来自 DAgger。
- 当前 phase/remaining 是 GT path index 比例，只能解释为粗粒度路线进度；recovery 是离轨时的逐 ghost 候选标签，不能与 off-route 状态等同。

## VisualEvidenceHead 冻结与辅助损失复核

- 采纳预训练专用视觉语义监督：预训练以 `1× LR` 训练 `VisualEvidenceHead`；DAgger 严格加载后冻结该 head、设 `visual loss=0`，不再创建 Semantic sensors。固定语义输出仍进入地图更新，`visual_category_projection` 继续训练。
- `L_state` 是累计地图残差、平均写门和 token 多样性的无 GT 正则；“写门预算”更准确地说是写门惩罚。
- `phase` 只是 `remaining` 的五档量化，目前保留为可消融项；`recovery` 已从 `L_progress` 拆为独立 `L_recovery`。
- DAgger 的进度是连续位置投影到单调最近 GT 点后的索引比例；预训练使用拓扑 viewpoint 索引，两阶段存在轻微定义差异。详见 [`07_状态进度与Ghost损失说明.md`](../../upload_files/07_状态进度与Ghost损失说明.md)。

## Panorama 语义缓存的 Habitat 位姿兼容修复

- 根因：Habitat-Lab 0.1.7 虽把 `position` 标注为 list，但 Habitat-Sim 0.1.7
  会将其直接传给 Magnum `translate()`；后者不接受 Python list。
- 修复：在缓存生成器边界把 connectivity position 和 identity rotation 显式转换为
  `float32 numpy.ndarray`，并拒绝过短或包含非有限位置值的 pose。
- 验证：在原报错场景 `Vt2qJdWjCF2` 上用 GPU 7 真实渲染 1 个 viewpoint，成功
  写出 shape `(37,)`、dtype `float32` 的 HDF5 标签；对应 3 个回归测试通过。

## OnlineFusion 最终契约同步

- 辅助损失固定为 `grid/state/visual/progress/recovery/ghost` 6 项；
  `L_recovery` 从 `L_progress` 独立，两者分别加权。
- `VisualEvidenceHead` 只读取现有 `gmap_img_fts` panorama 节点特征，不读取
  step/task/global-position embedding 或指令。
- map→ghost query 融合原图节点、instruction pool 和 `progress hidden`；图
  embedding 残差只写 ghost，STOP 使用独立 logit residual head。
- 预训练中 Map Encoder/OnlineFusion 用 `1× LR`、现有后端用 `0.1× LR`；
  DAgger 额外冻结 `VisualEvidenceHead`，并要求 `optimizer_profile=online_fusion`、
  `freeze_base=False`、`require_complete_pretrained_modules=True`。预训练基础 checkpoint
  只可缺少明确新增的 Map Encoder/OnlineFusion 子树，其余键/shape 严格
  匹配；DAgger 初始权重不允许静默缺键或 shape 不匹配。
  参数组完整覆盖 trainable tensors 且不重复。

## OnlineFusion 真实两阶段闭环

- 用 2.2 GB baseline checkpoint、正式 LLM-Grid/GT/语义缓存和真实样本，
  分别完成 SAP 与 MLM 的单步前向、六项辅助损失、反向、optimizer
  step、最小 R2R/RxR 验证和 checkpoint 保存；SAP/MLM loss 分别为
  `1.47995`/`0.62067`。
- 用上述完整预训练 checkpoint 完成 `1 env × 2 steps` 真实 DAgger：
  Habitat RGB-D、拓扑图、递归 `map_state`、teacher action、辅助损失、反向、
  optimizer step 和 DAgger checkpoint 保存均通过。LLM-Grid/GT 配对为
  `10707/10707`；`VisualEvidenceHead` 不在 DAgger 参数组中。
- 指定的 `etpr1-py38` 原缺少仓库已声明的
  `typed-argument-parser==1.10.1`，已补装；否则 `run.py` 会在进入
  DAgger 前报 `No module named tap`。
