# 2026-08-11

## Feature-fusion 交接快照

- [x] 固定新开发 checkout 为 `main@c442132a`，并记录原始 Try5、blurred Try5
      与 LLM-Grid DAgger 的历史 provenance commits。
- [x] 将接收端根目录修正为 `/data/xukai/etp-r1-snapshot/`。
- [x] 增加 fail-closed 传输脚本：先在新 snapshot root 核验三份 canonical DAgger
      checkpoint SHA-256，再将 PriorGT/LLM-Grid 的完整相关 cache namespaces
      打包、断点传输并在接收端执行 archive SHA-256 校验。
- [x] 删除错误的旧目录迁移假设；checkpoint 已直接上传到
      `/data/xukai/etp-r1-snapshot/checkpoints/`，脚本不再读取或复制旧 destination。
- [x] 按“完整交接”要求保留 boxes、predictions、status 与 pretraining cache；明确
      raw status 可能包含源机器绝对路径。
- [x] 完整 cache 限定为本次 DAgger handoff 实际使用的三个 namespace；不混入
      epoch-specific predictor caches 或独立 ETP-R1 pretraining namespaces。
- [x] 明确不重新分发 MP3D scenes 与 Llama weights，并记录共享 ETP-R1 runtime
      weights 的期望 SHA-256。

## Feature-fusion 预训练说明

- [x] 核对维护中的 map-enabled pretraining：`update_lang_bert=true`，optimizer
      接收全部 model parameters；语言、视觉/深度投影、global encoder、map
      encoder、Try5 graph-to-map attention 及 MLM/SAP heads 联合训练。
- [x] 动态核对 `current+prior_gt` 与 `try5+llm_grid`：registered frozen
      parameters 均为 0；source 只改变 cache input，不改变 trainability。
- [x] 明确固定边界：缓存 cognitive grids、预计算 RGB/depth features 与监督标签
      属于输入；用于 category projection 初始化的独立 CLIP model 固定且不进入
      optimizer，但 projection 本身可训练。
- [x] 在交接文档中区分 Try5 的单向融合、无 decoder/box loss，与 `current` 的
      双向融合及 box-supervised decoder，禁止将两者的 pretraining checkpoint
      当作等价结果。
- [x] 给出两阶段建议：先以 complete DAgger checkpoint 做显式 key-whitelist 的
      function-preserving zero-init residual 新 fusion、step-0 等价性及短程适配与
      梯度检查；若方案可行，再执行 source/architecture-matched pretraining 与
      matched DAgger。hard replacement 必须单独记录 step-0 regression；前者仅回答
      可适配性，不能替代公平的端到端比较。
- [x] 记录 pretrain-to-online strict transfer 风险：历史 unblurred pretrain 的 map
      schema 已旧，维护中的 pretrain/online fusion key path 也不同；新 pretraining
      之前必须用完整 load report 证明 map/fusion weights 实际落入 online policy。
